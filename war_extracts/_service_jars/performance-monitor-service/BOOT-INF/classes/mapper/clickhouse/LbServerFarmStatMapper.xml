<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.h3c.h3cloud.performance.monitor.service.mapper.clickhouse.LbServerFarmStatMapper">
    <delete id ="deleteBeforeTime">
        ALTER TABLE TBL_LB_SERVERFARM_STAT DELETE WHERE TIME &lt; #{time}
    </delete>
    <select id="readServerConnectionTrendByCondition" resultType="com.h3c.h3cloud.common.model.performance.LbServerFarmStatData">
        SELECT TOTAL_CONNECTIONS, TIME FROM TBL_LB_SERVERFARM_STAT
        WHERE DRX_ID =${drxId} AND TIME BETWEEN ${startTime} AND ${endTime} ORDER BY TIME ASC
    </select>
</mapper>
