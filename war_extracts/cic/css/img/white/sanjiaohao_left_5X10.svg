<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN"
"http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg width="5.0000" height="10.0000" viewBox="0.0000 0.0000 5.0000 10.0000"  version="1.1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" onload='Init(evt)'>
   <script type='text/ecmascript'><![CDATA[
          var SVGDocument = null;
          var SVGRoot = null;
          function Init(evt)
          {
                SVGDocument = evt.target.ownerDocument;
                 SVGRoot = SVGDocument.documentElement;
          }
         function Toggle(evt,targetId)
          {
                      var obj = evt.target;
         if (targetId)
         {
            obj = SVGDocument.getElementById(targetId);
         }
                id=obj.getAttributeNS(null, 'id');
                var myobj,i,myid,strs,params,Number,states,state,cur
                strs=id.split("(");
                params=strs[1].split(";");
                Number=strs[0];
                states=params[1];
                state=params[2];
                cur=params[3];
                states=parseInt(states);
                state=parseInt(state);
                if(state+1>=states)
                        state=0;
                else
                        state=state+1;
                for(i=0;i<states;i++)
                {
                        myid=Number+'('+params[0]+';'+states+';'+i+';'+cur+';'+params[4]+';'+params[5]+';'+params[6]+';'+params[7];
                        myobj=SVGDocument.getElementById(myid);
                        if(i==state)
                        {
                                myobj.setAttributeNS(null, 'visibility', 'visible');
                        }
                        else
                        {
                                myobj.setAttributeNS(null, 'visibility', 'hidden');
                        }
                }
        }
   ]]></script>
<path transform="matrix(-1.0000,0.0000,0.0000,-1.0000,2.5000,4.9585)" d="M2.5000,0.0715L-2.4160,-4.9585L-2.4790,-4.8965L-2.5000,4.9585Z" style="fill-opacity:1.0000;fill:#696F7F;stroke:none "/>

</svg>