# CVK Agent Python dependencies
# Core (no external deps required — stdlib-only by default)
urllib3>=2.0.0    # HTTP transport (Feign OkHttp3 equivalent)

# Optional: Real hardware integration
# libvirt-python>=9.0.0  # VM lifecycle via libvirt (optional)
# paramiko>=3.3.0         # SCP file transfer (optional)
# pika>=1.3.0             # RabbitMQ events (optional)

# Optional: Parsing
# lxml>=4.9.0             # Faster XML parsing (optional)
