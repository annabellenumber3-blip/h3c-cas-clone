m	Z	m
Abstract linear algebra library.
This module defines a class hierarchy that implements a kind of "lazy"
matrix representation, called the ``LinearOperator``. It can be used to do
linear algebra with extremely large sparse or structured matrices, without
representing those explicitly in memory. Such matrices can be added,
multiplied, transposed, etc.
As a motivating example, suppose you want have a matrix where almost all of
the elements have the value one. The standard sparse matrix representation
skips the storage of zeros, but not ones. By contrast, a LinearOperator is
able to represent such matrices efficiently. First, we need a compact way to
represent an all-ones matrix::
    >>> import numpy as np
    >>> class Ones(LinearOperator):
    ...     def __init__(self, shape):
    ...         super(Ones, self).__init__(dtype=None, shape=shape)
    ...     def _matvec(self, x):
    ...         return np.repeat(x.sum(), self.shape[0])
Instances of this class emulate ``np.ones(shape)``, but using a constant
amount of storage, independent of ``shape``. The ``_matvec`` method specifies
how this linear operator multiplies with (operates on) a vector. We can now
add this operator to a sparse matrix that stores only offsets from one::
    >>> from scipy.sparse import csr_matrix
    >>> offsets = csr_matrix([[1, 0, 2], [0, -1, 0], [0, 0, 3]])
    >>> A = aslinearoperator(offsets) + Ones(offsets.shape)
    >>> A.dot([1, 2, 3])
    array([13,  4, 15])
The result is the same as that given by its dense, explicitly-stored
counterpart::
    >>> (np.ones(A.shape, A.dtype) + offsets.toarray()).dot([1, 2, 3])
    array([13,  4, 15])
Several algorithms in the ``scipy.sparse`` library are able to operate on
``LinearOperator`` instances.
isspmatrix)
isshape
	isintlike
asmatrix
is_pydata_spmatrix
LinearOperator
aslinearoperatorc
d!d"
d#d$
d%d&
d'd(
d)d*
d+d,
d-d.
d/d0
d1d2
d3d4
Z d5d6
Common interface for performing matrix vector products
    Many iterative methods (e.g. cg, gmres) do not need to know the
    individual entries of a matrix to solve a linear system A*x=b.
    Such solvers only require the computation of matrix vector
    products, A*v where v is a dense vector.  This class serves as
    an abstract interface between iterative solvers and matrix-like
    objects.
    To construct a concrete LinearOperator, either pass appropriate
    callables to the constructor of this class, or subclass it.
    A subclass must implement either one of the methods ``_matvec``
    and ``_matmat``, and the attributes/properties ``shape`` (pair of
    integers) and ``dtype`` (may be None). It may call the ``__init__``
    on this class to have these attributes validated. Implementing
    ``_matvec`` automatically implements ``_matmat`` (using a naive
    algorithm) and vice-versa.
    Optionally, a subclass may implement ``_rmatvec`` or ``_adjoint``
    to implement the Hermitian adjoint (conjugate transpose). As with
    ``_matvec`` and ``_matmat``, implementing either ``_rmatvec`` or
    ``_adjoint`` implements the other automatically. Implementing
    ``_adjoint`` is preferable; ``_rmatvec`` is mostly there for
    backwards compatibility.
    Parameters
    ----------
    shape : tuple
        Matrix dimensions (M, N).
    matvec : callable f(v)
        Returns returns A * v.
    rmatvec : callable f(v)
        Returns A^H * v, where A^H is the conjugate transpose of A.
    matmat : callable f(V)
        Returns A * V, where V is a dense matrix with dimensions (N, K).
    dtype : dtype
        Data type of the matrix.
    rmatmat : callable f(V)
        Returns A^H * V, where V is a dense matrix with dimensions (M, K).
    Attributes
    ----------
    args : tuple
        For linear operators describing products etc. of other linear
        operators, the operands of the binary operation.
    ndim : int
        Number of dimensions (this is always 2)
    See Also
    --------
    aslinearoperator : Construct LinearOperators
    Notes
    -----
    The user-defined matvec() function must properly handle the case
    where v has shape (N,) as well as the (N,1) case.  The shape of
    the return type is handled internally by LinearOperator.
    LinearOperator instances can also be multiplied, added with each
    other and exponentiated, all lazily: the result of these operations
    is always a new, composite LinearOperator, that defers linear
    operations to the original operators and combines the results.
    More details regarding how to subclass a LinearOperator and several
    examples of concrete LinearOperator instances can be found in the
    external project `PyLops <https://pylops.readthedocs.io>`_.
    Examples
    --------
    >>> import numpy as np
    >>> from scipy.sparse.linalg import LinearOperator
    >>> def mv(v):
    ...     return np.array([2*v[0], 3*v[1]])
    ...
    >>> A = LinearOperator((2,2), matvec=mv)
    >>> A
    <2x2 _CustomLinearOperator with dtype=float64>
    >>> A.matvec(np.ones(2))
    array([ 2.,  3.])
    >>> A * np.ones(2)
    array([ 2.,  3.])
    
NzMLinearOperator subclass should implement at least one of _matvec and _matmat.r	
category
stacklevel)
super
__new__
_CustomLinearOperator
type
_matvec
_matmat
warnings
warn
RuntimeWarning)
args
kwargs
	__class__
C/usr/lib64/python3.9/site-packages/scipy/sparse/linalg/interface.pyr
LinearOperator.__new__c
Initialize this LinearOperator.
        To be called by subclasses. ``dtype`` may be None; ``shape`` should
        be convertible to a length-2 tuple.
        Nz
invalid shape %r (must be 2-d))
dtype
tupler
ValueError
shape)
selfr
__init__
LinearOperator.__init__c
zCCalled from subclasses at the end of the __init__ routine.
        N
zerosr!
asarray
matvec)
_init_dtype
LinearOperator._init_dtypec
Default matrix-matrix multiplication handler.
        Falls back on the user-defined _matvec method, so defining that will
        define matrix multiplication (though in a very suboptimal way).
        c
reshape
<listcomp>
z*LinearOperator._matmat.<locals>.<listcomp>)
hstack
LinearOperator._matmatc
Default matrix-vector multiplication handler.
        If self is a linear operator of shape (M, N), then this method will
        be called on a shape (N,) or (N, 1) ndarray, and should return a
        shape (M,) or (M, 1) ndarray.
        This default implementation falls back on _matmat, so defining that
        will define matrix-vector multiplication as well.
        r$
matmatr+
LinearOperator._matvecc
Matrix-vector multiplication.
        Performs the operation y=A*x where A is an MxN linear
        operator and x is a column vector or 1-d array.
        Parameters
        ----------
        x : {matrix, ndarray}
            An array with shape (N,) or (N,1).
        Returns
        -------
        y : {matrix, ndarray}
            A matrix or ndarray with shape (M,) or (M,1) depending
            on the type and shape of the x argument.
        Notes
        -----
        This matvec wraps the user-specified matvec routine or overridden
        _matvec method to ensure that y has the correct shape and type.
        r*
dimension mismatchr	
z/invalid shape returned by user-defined matvec())
asanyarrayr!
isinstance
matrixr
ndimr+
LinearOperator.matvecc
Adjoint matrix-vector multiplication.
        Performs the operation y = A^H * x where A is an MxN linear
        operator and x is a column vector or 1-d array.
        Parameters
        ----------
        x : {matrix, ndarray}
            An array with shape (M,) or (M,1).
        Returns
        -------
        y : {matrix, ndarray}
            A matrix or ndarray with shape (N,) or (N,1) depending
            on the type and shape of the x argument.
        Notes
        -----
        This rmatvec wraps the user-specified rmatvec routine or overridden
        _rmatvec method to ensure that y has the correct shape and type.
        r*
z0invalid shape returned by user-defined rmatvec())
_rmatvecr;
rmatvec
LinearOperator.rmatvecc
z6Default implementation of _rmatvec; defers to adjoint.N)
_adjointr
NotImplementedError
LinearOperator._rmatvecc
Matrix-matrix multiplication.
        Performs the operation y=A*X where A is an MxN linear
        operator and X dense N*K matrix or ndarray.
        Parameters
        ----------
        X : {matrix, ndarray}
            An array with shape (N,K).
        Returns
        -------
        Y : {matrix, ndarray}
            A matrix or ndarray with shape (M,K) depending on
            the type of the X argument.
        Notes
        -----
        This matmat wraps any user-specified matmat routine or overridden
        _matmat method to ensure that y has the correct type.
        r	
(expected 2-d ndarray or matrix, not %d-dr
dimension mismatch: %r, %r)	r
LinearOperator.matmatc
Adjoint matrix-matrix multiplication.
        Performs the operation y = A^H * x where A is an MxN linear
        operator and x is a column vector or 1-d array, or 2-d array.
        The default implementation defers to the adjoint.
        Parameters
        ----------
        X : {matrix, ndarray}
            A matrix or 2D array.
        Returns
        -------
        Y : {matrix, ndarray}
            A matrix or 2D array depending on the type of the input.
        Notes
        -----
        This rmatmat wraps the user-specified rmatmat routine.
        r	
_rmatmatr;
rmatmatX
LinearOperator.rmatmatc
z@Default implementation of _rmatmat defers to rmatvec or adjoint.c
z+LinearOperator._rmatmat.<locals>.<listcomp>N)
LinearOperator._rmatmatc
__call__
LinearOperator.__call__c
dotr7
__mul__
LinearOperator.__mul__c
Matrix-matrix or matrix-vector multiplication.
        Parameters
        ----------
        x : array_like
            1-d or 2-d array, representing a vector or matrix.
        Returns
        -------
        Ax : array
            1-d or 2-d array (depending on the shape of x) that represents
            the result of applying this linear operator on x.
        r*
z+expected 1-d or 2-d array or matrix, got %rN)
_ProductLinearOperatorr
isscalar
_ScaledLinearOperatorr%
LinearOperator.dotc
Nz0Scalar operands are not allowed, use '*' instead)
otherr
__matmul__
LinearOperator.__matmul__c
__rmul__rU
__rmatmul__
LinearOperator.__rmatmul__c
NotImplementedr7
LinearOperator.__rmul__c
_PowerLinearOperatorrZ
__pow__
LinearOperator.__pow__c
_SumLinearOperatorrZ
__add__
LinearOperator.__add__c
__neg__
LinearOperator.__neg__c
__sub__
LinearOperator.__sub__c
unspecified dtypez
dtype=z
<%dx%d %s with %s>)
strr
__name__)
__repr__
LinearOperator.__repr__c
Hermitian adjoint.
        Returns the Hermitian adjoint of self, aka the Hermitian
        conjugate or Hermitian transpose. For a complex matrix, the
        Hermitian adjoint is equal to the conjugate transpose.
        Can be abbreviated self.H instead of self.adjoint().
        Returns
        -------
        A_H : LinearOperator
            Hermitian adjoint of self.
        )
adjoint
LinearOperator.adjointc
Transpose this linear operator.
        Returns a LinearOperator that represents the transpose of this one.
        Can be abbreviated self.T instead of self.transpose().
        )
_transposer/
	transpose
LinearOperator.transposec
z6Default implementation of _adjoint; defers to rmatvec.)
_AdjointLinearOperatorr/
LinearOperator._adjointc
z? Default implementation of _transpose; defers to rmatvec + conj)
_TransposedLinearOperatorr/
LinearOperator._transpose)#rc
__module__
__qualname__
__doc__r=
propertyrF
__classcell__r
z>Linear operator defined in terms of user-specified operations.Nc
"_CustomLinearOperator__matvec_impl
#_CustomLinearOperator__rmatvec_impl
#_CustomLinearOperator__rmatmat_impl
"_CustomLinearOperator__matmat_implr(
_CustomLinearOperator.__init__c
_CustomLinearOperator._matmatc
_CustomLinearOperator._matvecc
rmatvec is not defined)
funcr
_CustomLinearOperator._rmatvecc
_CustomLinearOperator._rmatmatc
_CustomLinearOperator._adjoint)
NNNN)
z$Adjoint of arbitrary Linear Operatorc
_AdjointLinearOperator.__init__c
_AdjointLinearOperator._matvecc
_AdjointLinearOperator._rmatvecc
_AdjointLinearOperator._matmatc
_AdjointLinearOperator._rmatmat
z*Transposition of arbitrary Linear Operatorc
z"_TransposedLinearOperator.__init__c
conjru
z!_TransposedLinearOperator._matvecc
z"_TransposedLinearOperator._rmatvecc
z!_TransposedLinearOperator._matmatc
z"_TransposedLinearOperator._rmatmatrw
hasattr
appendr
find_common_type)
Z	operatorsZ
dtypesr
_get_dtypeS
)both operands have to be a LinearOperatorz$cannot add %r and %r: shape mismatch)	r;
_SumLinearOperator.__init__c
_SumLinearOperator._matvecc
_SumLinearOperator._rmatvecc
_SumLinearOperator._rmatmatc
_SumLinearOperator._matmatc
_SumLinearOperator._adjoint
z)cannot multiply %r and %r: shape mismatch)	r;
_ProductLinearOperator.__init__c
_ProductLinearOperator._matvecc
_ProductLinearOperator._rmatvecc
_ProductLinearOperator._rmatmatc
_ProductLinearOperator._matmatc
_ProductLinearOperator._adjointr
LinearOperator expected as Az
scalar expected as alpha)
alphar
_ScaledLinearOperator.__init__c
_ScaledLinearOperator._matvecc
_ScaledLinearOperator._rmatvecc
_ScaledLinearOperator._rmatmatc
_ScaledLinearOperator._matmatc
_ScaledLinearOperator._adjointr
z&square LinearOperator expected, got %rz"non-negative integer expected as p)
_PowerLinearOperator.__init__c
copyr*
array
ranger
funr8
_power
_PowerLinearOperator._powerc
_PowerLinearOperator._matvecc
_PowerLinearOperator._rmatvecc
_PowerLinearOperator._rmatmatc
_PowerLinearOperator._matmatc
_PowerLinearOperator._adjoint)
MatrixLinearOperatorc
_MatrixLinearOperator__adjr
MatrixLinearOperator.__init__c
MatrixLinearOperator._matmatc
_AdjointMatrixOperatorr/
MatrixLinearOperator._adjoint)
_AdjointMatrixOperator__adjointr
_AdjointMatrixOperator.__init__c
_AdjointMatrixOperator.dtypec
_AdjointMatrixOperator._adjointN)
IdentityOperatorNc
IdentityOperator.__init__c
IdentityOperator._matvecc
IdentityOperator._rmatvecc
IdentityOperator._rmatmatc
IdentityOperator._matmatc
IdentityOperator._adjoint)
rht	|
Return A as a LinearOperator.
    'A' may be any of the following types:
     - ndarray
     - matrix
     - sparse matrix (e.g. csr_matrix, lil_matrix, etc.)
     - LinearOperator
     - An object with .shape and .matvec attributes
    See the LinearOperator documentation for additional information.
    Notes
    -----
    If 'A' has no .dtype attribute, the data type is determined by calling
    :func:`LinearOperator.matvec()` - set the .dtype attribute to prevent this
    call upon the linear operator creation.
    Examples
    --------
    >>> from scipy.sparse.linalg import aslinearoperator
    >>> M = np.array([[1,2,3],[4,5,6]], dtype=np.int32)
    >>> aslinearoperator(M)
    <2x3 MatrixLinearOperator with dtype=int32>
    r	
array must have ndim <= 2r!
type not understood)
ndarrayr<
atleast_2dr%
	TypeError)
numpyr
scipy.sparser
scipy.sparse.sputilsr
__all__
objectr
<module>
