Disjoint set data structure
DisjointSeta
 Disjoint set data structure for incremental connectivity queries.
    .. versionadded: 1.6.0
    Attributes
    ----------
    n_subsets : int
        The number of subsets.
    Methods
    -------
    add
    merge
    connected
    subset
    subsets
    __getitem__
    Notes
    -----
    This class implements the disjoint set [1]_, also known as the *union-find*
    or *merge-find* data structure. The *find* operation (implemented in
    `__getitem__`) implements the *path halving* variant. The *merge* method
    implements the *merge by size* variant.
    References
    ----------
    .. [1] https://en.wikipedia.org/wiki/Disjoint-set_data_structure
    Examples
    --------
    >>> from scipy.cluster.hierarchy import DisjointSet
    Initialize a disjoint set:
    >>> disjoint_set = DisjointSet([1, 2, 3, 'a', 'b'])
    Merge some subsets:
    >>> disjoint_set.merge(1, 2)
    True
    >>> disjoint_set.merge(3, 'a')
    True
    >>> disjoint_set.merge('a', 'b')
    True
    >>> disjoint_set.merge('b', 'b')
    False
    Find root elements:
    >>> disjoint_set[2]
    1
    >>> disjoint_set['b']
    3
    Test connectivity:
    >>> disjoint_set.connected(1, 2)
    True
    >>> disjoint_set.connected(1, 'b')
    False
    List elements in disjoint set:
    >>> list(disjoint_set)
    [1, 2, 3, 'a', 'b']
    Get the subset containing 'a':
    >>> disjoint_set.subset('a')
    {'a', 3, 'b'}
    Get all subsets in the disjoint set:
    >>> disjoint_set.subsets()
    [{1, 2}, {'a', 3, 'b'}]
    Nc
	n_subsets
_sizes
_parents
_nbrs
collections
OrderedDict
_indices
add)
self
elements
>/usr/lib64/python3.9/site-packages/scipy/_lib/_disjoint_set.py
__init__U
DisjointSet.__init__c
zsReturns an iterator of the elements in the disjoint set.
        Elements are ordered by insertion order.
        )
iterr	
__iter__b
DisjointSet.__iter__c
lenr	
__len__i
DisjointSet.__len__c
__contains__l
DisjointSet.__contains__c
Find the root element of `x`.
        Parameters
        ----------
        x : hashable object
            Input element.
        Returns
        -------
        root : hashable object
            Root element of `x`.
        )
KeyErrorr
parentsr
__getitem__o
DisjointSet.__getitem__c
z(Add element `x` to disjoint set
        N
DisjointSet.addc
Merge the subsets of `x` and `y`.
        The smaller subset (the child) is merged into the larger subset (the
        parent). If the subsets are of equal size, the root element which was
        first inserted into the disjoint set is selected as the parent.
        Parameters
        ----------
        x, y : hashable object
            Elements to merge.
        Returns
        -------
        merged : bool
            True if `x` and `y` were in disjoint sets, False otherwise.
        Fr
sizesr
merge
DisjointSet.mergec
Test whether `x` and `y` are in the same subset.
        Parameters
        ----------
        x, y : hashable object
            Elements to test.
        Returns
        -------
        result : bool
            True if `x` and `y` are in the same set, False otherwise.
        r
	connected
DisjointSet.connectedc
Get the subset containing `x`.
        Parameters
        ----------
        x : hashable object
            Input element.
        Returns
        -------
        result : set
            Subset containing `x`.
        )
append
set)
resultZ
nxtr
subset
DisjointSet.subsetc
Get all the subsets in the disjoint set.
        Returns
        -------
        result : set
            Subsets in the disjoint set.
        )
updater!
visitedr
xsetr
subsets
DisjointSet.subsets)
__name__
__module__
__qualname__
__doc__r
<module>
