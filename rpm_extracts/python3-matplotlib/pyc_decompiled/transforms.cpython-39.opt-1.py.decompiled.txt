m	Z	
d!d"
d#d$
d%d&
Z!d'd(
d)d*
d+d,
Z$d-d.
d/d0
d1d2
d3d4
d4e'
d5d6
d7d8
d9d:
d;d<
d=d>
d>e,
Z-dOdBdC
Z.dDdE
Z/dPdGdH
Z0dIdJ
Z1dQdMdN
Matplotlib includes a framework for arbitrary geometric
transformations that is used determine the final position of all
elements drawn on the canvas.
Transforms are composed into trees of `TransformNode` objects
whose actual value depends on their children.  When the contents of
children change, their parents are automatically invalidated.  The
next time an invalidated transform is accessed, it is recomputed to
reflect those changes.  This invalidation/caching approach prevents
unnecessary recomputations of transforms, and contributes to better
interactive performance.
For example, here is a graph of the transform tree used to plot data
to the graph:
.. image:: ../_static/transforms.png
The framework can be used for both affine and non-affine
transformations.  However, for speed, we want use the backend
renderers to perform affine transformations whenever possible.
Therefore, it is possible to perform just the affine or non-affine
part of a transformation on a set of data.  The affine is always
assumed to occur after the non-affine.  For any transform::
  full transform == non-affine part + affine part
The backends are not expected to handle non-affine transformations
themselves.
See the tutorial :doc:`/tutorials/advanced/transforms_tutorial` for examples
of how to use transforms.
inv)
_api)
affine_transform
count_bboxes_overlapping_bbox
update_path_extents
PathFc
    Generate a ``__str__`` method for a `.Transform` subclass.
    After ::
        class T:
            __str__ = _make_str_method("attr", key="other")
    ``str(T(...))`` will be
    .. code-block:: text
        {type(T).__name__}(
            {self.attr},
            key={self.other})
    z
    )
prefixc
isinstance
repr)
;/usr/lib64/python3.9/site-packages/matplotlib/transforms.py
strreprJ
z!_make_str_method.<locals>.strreprc
getattr)
indent
selfr
	<genexpr>M
z5_make_str_method.<locals>.<lambda>.<locals>.<genexpr>c
type
__name__
join
items
argsr
kwargsr
<lambda>K
z"_make_str_method.<locals>.<lambda>)
	functools
partial
textwrapr
_make_str_method8
TransformNodea'
    The base class for anything that participates in the transform tree
    and needs to invalidate its parents or be invalidated.  This includes
    classes that are not really transforms, such as bounding boxes, since some
    transforms depend on bounding boxes to compute their values.
    r
        Parameters
        ----------
        shorthand_name : str
            A string representing the "name" of the transform. The name carries
            no significance other than to improve the readability of
            ``str(transform)`` when DEBUG=True.
        r
_parents
_invalid
_shorthand_name)
shorthand_namer
__init__n
TransformNode.__init__c
__str__
TransformNode.__str__c
<dictcomp>
z.TransformNode.__getstate__.<locals>.<dictcomp>
__dict__r1
__getstate__
TransformNode.__getstate__c
popr
z7TransformNode.__setstate__.<locals>.<dictcomp>.<lambda>)
weakref
refr1
z.TransformNode.__setstate__.<locals>.<dictcomp>r:
Z	data_dictr
__setstate__
TransformNode.__setstate__c
copy
superr1
varsr$
set_children)
other
	__class__r
__copy__
TransformNode.__copy__c
        Invalidate this `TransformNode` and triggers an invalidation of its
        ancestors.  Should be called any time the transform changes.
        )
invalidating_node)
INVALID
	is_affine
INVALID_AFFINE
_invalidate_internal
valuer
invalidate
TransformNode.invalidatec
        Called by :meth:`invalidate` and subsequently ascends the transform
        stack calling each TransformNode's _invalidate_internal method.
        N
pass_through
listr1
valuesrR
status_changed
parentr
z"TransformNode._invalidate_internalc
        Set the children of the transform, to let the invalidation
        system know which transforms can invalidate this transform.
        Should be called from the constructor of any transforms that
        depend on other transforms.
        c
z,TransformNode.set_children.<locals>.<lambda>N)
children
childrA
TransformNode.set_childrenc
        Return a frozen copy of this transform node.  The frozen copy will not
        be updated when its children change.  Useful for storing a previously
        known state of a transform where ``copy.deepcopy()`` might normally be
        used.
        r
frozen
TransformNode.frozen)
__module__
__qualname__
__doc__
INVALID_NON_AFFINErQ
is_bboxrW
DEBUGr6
__classcell__r
d!d"
d#d$
d%d&
d'd(
d)d*
d+d,
d-d.
d/d0
Z d1d2
Z!d3d4
Z"d5d6
Z#d7d8
Z$d9d:
Z%d;d<
Z&d=d>
Z'd?d@
Z(dAdB
Z)dCdDdEdFdGdHdIdJdKdL
	Z*dkdNdO
Z+dPdQ
Z,dldSdT
Z-dUdV
Z.dWdX
Z/dYdZ
Z0d[d\
Z1d]d^
Z2d_d`
Z3dadb
Z4dcdd
Z5dedf
dgdh
didj
Z8dMS
BboxBasea`
    The base class of all bounding boxes.
    This class is immutable; `Bbox` is a mutable subclass.
    The canonical representation is as two points, with no
    restrictions on their ordering.  Convenience properties are
    provided to get the left, bottom, right and top edges and width
    and height, but these are not stored explicitly.
    Tc
Bbox bounds are a masked array.r
Singular Bbox.)
MaskedArrayr
warn_external
asarray
any)
pointsr
_check
BboxBase._checkc
Bbox
get_pointsrC
BboxBase.frozenc
	__array__
BboxBase.__array__c
        The first of the pair of *x* coordinates that define the bounding box.
        This is not guaranteed to be less than :attr:`x1` (for that, use
        :attr:`xmin`).
        
BboxBase.x0c
        The first of the pair of *y* coordinates that define the bounding box.
        This is not guaranteed to be less than :attr:`y1` (for that, use
        :attr:`ymin`).
        
BboxBase.y0c
        The second of the pair of *x* coordinates that define the bounding box.
        This is not guaranteed to be greater than :attr:`x0` (for that, use
        :attr:`xmax`).
        
BboxBase.x1c
        The second of the pair of *y* coordinates that define the bounding box.
        This is not guaranteed to be greater than :attr:`y0` (for that, use
        :attr:`ymax`).
        
BboxBase.y1c
        The first pair of (*x*, *y*) coordinates that define the bounding box.
        This is not guaranteed to be the bottom-left corner (for that, use
        :attr:`min`).
        r
BboxBase.p0c
        The second pair of (*x*, *y*) coordinates that define the bounding box.
        This is not guaranteed to be the top-right corner (for that, use
        :attr:`max`).
        r
BboxBase.p1c
z"The left edge of the bounding box.Nr
minrn
xmin9
BboxBase.xminc
z$The bottom edge of the bounding box.Nr
ymin>
BboxBase.yminc
z#The right edge of the bounding box.Nr
maxrn
xmaxC
BboxBase.xmaxc
z!The top edge of the bounding box.Nr
ymaxH
BboxBase.ymaxc
z+The bottom-left corner of the bounding box.r
axisr|
BboxBase.minc
z)The top-right corner of the bounding box.r
BboxBase.maxc
        The pair of *x* coordinates that define the bounding box.
        This is not guaranteed to be sorted from left to right.
        Nr
	intervalxW
BboxBase.intervalxc
        The pair of *y* coordinates that define the bounding box.
        This is not guaranteed to be sorted from bottom to top.
        Nr
	intervaly`
BboxBase.intervalyc
z'The (signed) width of the bounding box.rv
widthi
BboxBase.widthc
z(The (signed) height of the bounding box.rx
heighto
BboxBase.heightc
z2The (signed) width and height of the bounding box.r
sizeu
BboxBase.sizec
z?Return (:attr:`x0`, :attr:`y0`, :attr:`width`, :attr:`height`).ro
bounds{
BboxBase.boundsc
z8Return (:attr:`x0`, :attr:`y0`, :attr:`x1`, :attr:`y1`).)
flattenr%
extents
BboxBase.extentsc
NotImplementedErrorr%
BboxBase.get_pointsc
        Return whether *x* is in the closed (:attr:`x0`, :attr:`x1`) interval.
        
	containsx
BboxBase.containsxc
        Return whether *y* is in the closed (:attr:`y0`, :attr:`y1`) interval.
        
	containsy
BboxBase.containsyc
        Return whether ``(x, y)`` is in the bounding box or on its edge.
        )
contains
BboxBase.containsc
        Return whether this bounding box overlaps with the other bounding box.
        Parameters
        ----------
        other : `.BboxBase`
        
ax1Z
ay1Z
ax2Z
ay2Z
bx1Z
by1Z
bx2Z
by2r
overlaps
BboxBase.overlapsc
        Return whether *x* is in the open (:attr:`x0`, :attr:`x1`) interval.
        r
fully_containsx
BboxBase.fully_containsxc
        Return whether *y* is in the open (:attr:`y0`, :attr:`y1`) interval.
        r
fully_containsy
BboxBase.fully_containsyc
        Return whether ``x, y`` is in the bounding box, but not on its edge.
        )
fully_contains
BboxBase.fully_containsc
        Return whether this bounding box overlaps with the other bounding box,
        not including the edges.
        Parameters
        ----------
        other : `.BboxBase`
        r
fully_overlaps
BboxBase.fully_overlapsc
        Construct a `Bbox` by statically transforming this one by *transform*.
        r
	transformre
arrayrm
ptsZ
transformed
BboxBase.transformed)
        Return a copy of the `Bbox` anchored to *c* within *container*.
        Parameters
        ----------
        c : (float, float) or {'C', 'SW', 'S', 'SE', 'E', 'NE', ...}
            Either an (*x*, *y*) pair of relative coordinates (0 is left or
            bottom, 1 is right or top), 'C' (center), or a cardinal direction
            ('SW', southwest, is bottom left, etc.).
        container : `Bbox`, optional
            The box within which the `Bbox` is positioned; it defaults
            to the initial `Bbox`.
        See Also
        --------
        .Axes.set_anchor
        N)
coefsrm
_points)
	container
anchored
BboxBase.anchoredc
        Return a copy of the `Bbox`, shrunk by the factor *mx*
        in the *x* direction and the factor *my* in the *y* direction.
        The lower left corner of the box remains unchanged.  Normally
        *mx* and *my* will be less than 1, but this is not enforced.
        r
shrunk
BboxBase.shrunkr
        Return a copy of the `Bbox`, shrunk so that it is as
        large as it can be while having the desired aspect ratio,
        *box_aspect*.  If the box coordinates are relative (i.e.
        fractions of a larger box such as a figure) then the
        physical aspect ratio of that figure is specified with
        *fig_aspect*, so that *box_aspect* can also be given as a
        ratio of the absolute dimensions, not the relative dimensions.
        r
z.'box_aspect' and 'fig_aspect' must be positiveN)
ValueErrorr
box_aspectr
fig_aspectr
shrunk_to_aspect
BboxBase.shrunk_to_aspectc
        Return a list of new `Bbox` objects formed by splitting the original
        one with vertical lines at fractional positions given by *args*.
        r
xf0Z
<listcomp>6
z#BboxBase.splitx.<locals>.<listcomp>N
zip)
xfrw
splitx.
BboxBase.splitxc
        Return a list of new `Bbox` objects formed by splitting the original
        one with horizontal lines at fractional positions given by *args*.
        r
yf0Z
z#BboxBase.splity.<locals>.<listcomp>Nr
yfry
splity9
BboxBase.splityc
        Count the number of vertices contained in the `Bbox`.
        Any vertices with a non-finite x or y value are ignored.
        Parameters
        ----------
        vertices : Nx2 Numpy array.
        r
ignore)
invalidr
lenre
errstater}
sum)
verticesr
count_containsD
BboxBase.count_containsc
        Count the number of bounding boxes that overlap this one.
        Parameters
        ----------
        bboxes : sequence of `.BboxBase`
        c
z+BboxBase.count_overlaps.<locals>.<listcomp>)
atleast_3d)
bboxesr
count_overlapsT
BboxBase.count_overlapsc
        Construct a `Bbox` by expanding this one around its center by the
        factors *sw* and *sh*.
        g
deltawZ
deltah
expanded_
BboxBase.expandedc
z@Construct a `Bbox` by padding this one on all four sides by *p*.)
paddedk
BboxBase.paddedc
z<Construct a `Bbox` by translating this one by *tx* and *ty*.)
translatedp
BboxBase.translatedc
        Return the corners of this rectangle as an array of points.
        Specifically, this returns the array
        ``[[x0, y0], [x0, y1], [x1, y0], [x1, y1]]``.
        )
cornerst
BboxBase.cornersc
        Return the axes-aligned bounding box that bounds the result of rotating
        this `Bbox` by an angle of *radians*.
        T)
Affine2D
rotater
unit
update_from_data_xy)
radiansr
corners_rotated
bboxr
rotated~
BboxBase.rotatedc
z8Return a `Bbox` that contains all of the given *bboxes*.z
'bboxes' cannot be emptyc
z"BboxBase.union.<locals>.<listcomp>c
union
BboxBase.unionc
        Return the intersection of *bbox1* and *bbox2* if they intersect, or
        None if they don't.
        N)
maximumr~
minimumr
bbox1Z
bbox2rs
intersection
BboxBase.intersection)
)9r"
staticmethodrl
propertyrs
e	d	d
dJd d!
dKd"d#
d$d%
d&d'
d(d)
d*d+
d,d-
d.d/
d0d1
d2d3
d4d5
Z e!d6d7
Z"e!d8d9
Z#e!d:d;
Z$d<d=
Z%d>d?
Z&d@dA
Z'dBdC
Z(dDdE
Z)dFdG
)Lrm
    A mutable bounding box.
    Examples
    --------
    **Create from known bounds**
    The default constructor takes the boundary "points" ``[[xmin, ymin],
    [xmax, ymax]]``.
        >>> Bbox([[1, 1], [3, 7]])
        Bbox([[1.0, 1.0], [3.0, 7.0]])
    Alternatively, a Bbox can be created from the flattened points array, the
    so-called "extents" ``(xmin, ymin, xmax, ymax)``
        >>> Bbox.from_extents(1, 1, 3, 7)
        Bbox([[1.0, 1.0], [3.0, 7.0]])
    or from the "bounds" ``(xmin, ymin, width, height)``.
        >>> Bbox.from_bounds(1, 1, 2, 6)
        Bbox([[1.0, 1.0], [3.0, 7.0]])
    **Create from collections of points**
    The "empty" object for accumulating Bboxs is the null bbox, which is a
    stand-in for the empty set.
        >>> Bbox.null()
        Bbox([[inf, inf], [-inf, -inf]])
    Adding points to the null bbox will give you the bbox of those points.
        >>> box = Bbox.null()
        >>> box.update_from_data_xy([[1, 1]])
        >>> box
        Bbox([[1.0, 1.0], [1.0, 1.0]])
        >>> box.update_from_data_xy([[2, 3], [3, 2]], ignore=False)
        >>> box
        Bbox([[1.0, 1.0], [3.0, 3.0]])
    Setting ``ignore=True`` is equivalent to starting over from a null bbox.
        >>> box.update_from_data_xy([[1, 1]], ignore=True)
        >>> box
        Bbox([[1.0, 1.0], [1.0, 1.0]])
    .. warning::
        It is recommended to always specify ``ignore`` explicitly.  If not, the
        default value of ``ignore`` can be changed at any time by code with
        access to your Bbox, for example using the method `~.Bbox.ignore`.
    **Properties of the ``null`` bbox**
    .. note::
        The current behavior of `Bbox.null()` may be surprising as it does
        not have all of the properties of the "empty set", and as such does
        not behave like a "zero" object in the mathematical sense. We may
        change that in the future (with a deprecation period).
    The null bbox is the identity for intersections
        >>> Bbox.intersection(Bbox([[1, 1], [3, 7]]), Bbox.null())
        Bbox([[1.0, 1.0], [3.0, 7.0]])
    except with itself, where it returns the full space.
        >>> Bbox.intersection(Bbox.null(), Bbox.null())
        Bbox([[-inf, -inf], [inf, inf]])
    A union containing null will always return the full space (not the other
    set!)
        >>> Bbox.union([Bbox([[0, 0], [0, 0]]), Bbox.null()])
        Bbox([[-inf, -inf], [inf, inf]])
    c
        Parameters
        ----------
        points : ndarray
            A 2x2 numpy array of the form ``[[x0, y0], [x1, y1]]``.
        
z7Bbox points must be of the form "[[x0, y0], [x1, y1]]".TN)
float
shaper
_minpos
_ignorerC
_points_orig
Bbox.__init__c
	___init__r
Bbox.invalidatec
minposrC
frozen_bboxrK
Bbox.frozenc
z/Create a new unit `Bbox` from (0, 0) to (1, 1).r
z	Bbox.unitc
z9Create a new null `Bbox` from (inf, inf) to (-inf, -inf).)
null
z	Bbox.nullc
        Create a new `Bbox` from *x0*, *y0*, *width* and *height*.
        *width* and *height* may be negative.
        )
from_extents)
from_bounds 
Bbox.from_boundsN)
        Create a new Bbox from *left*, *bottom*, *right* and *top*.
        The *y*-axis increases upwards.
        Parameters
        ----------
        left, bottom, right, top : float
            The four extents of the bounding box.
        minpos : float or None
           If this is supplied, the Bbox will have a minimum positive value
           set. This is useful when dealing with logarithmic scales and other
           scales where negative bounds result in floating point errors.
        r
reshaper
Bbox.from_extentsc
Nz@Bbox(x0={0.x0:{1}}, y0={0.y0:{1}}, x1={0.x1:{1}}, y1={0.y1:{1}})
format)
fmtr
__format__?
Bbox.__format__c
Bbox.__str__c
Nz*Bbox([[{0.x0}, {0.y0}], [{0.x1}, {0.y1}]])r
__repr__G
Bbox.__repr__c
        Set whether the existing bounds of the box should be ignored
        by subsequent calls to :meth:`update_from_data_xy`.
        value : bool
           - When ``True``, subsequent calls to :meth:`update_from_data_xy`
             will ignore the existing bounds of the `Bbox`.
           - When ``False``, subsequent calls to :meth:`update_from_data_xy`
             will include the existing bounds of the `Bbox`.
        N)
Bbox.ignoreTc
        Update the bounds of the `Bbox` to contain the vertices of the
        provided path. After updating, the bounds will have positive *width*
        and *height*; *x0* and *y0* will be the minimal values.
        Parameters
        ----------
        path : `~matplotlib.path.Path`
        ignore : bool, optional
           - when ``True``, ignore the existing bounds of the `Bbox`.
           - when ``False``, include the existing bounds of the `Bbox`.
           - when ``None``, use the last value passed to :meth:`ignore`.
        updatex, updatey : bool, default: True
            When ``True``, update the x/y values.
        Nr
pathr
updatex
updateyrk
changedr
update_from_pathX
Bbox.update_from_pathc
        Update the x-bounds of the `Bbox` based on the passed in data. After
        updating, the bounds will have positive *width*, and *x0* will be the
        minimal value.
        Parameters
        ----------
        x : ndarray
            Array of x-values.
        ignore : bool, optional
           - When ``True``, ignore the existing bounds of the `Bbox`.
           - When ``False``, include the existing bounds of the `Bbox`.
           - When ``None``, use the last value passed to :meth:`ignore`.
        F)
ravelr
column_stack
onesr
update_from_data_x|
Bbox.update_from_data_xc
        Update the y-bounds of the `Bbox` based on the passed in data. After
        updating, the bounds will have positive *height*, and *y0* will be the
        minimal value.
        Parameters
        ----------
        y : ndarray
            Array of y-values.
        ignore : bool, optional
           - When ``True``, ignore the existing bounds of the `Bbox`.
           - When ``False``, include the existing bounds of the `Bbox`.
           - When ``None``, use the last value passed to :meth:`ignore`.
        F)
update_from_data_y
Bbox.update_from_data_yc
        Update the bounds of the `Bbox` based on the passed in data. After
        updating, the bounds will have positive *width* and *height*;
        *x0* and *y0* will be the minimal values.
        Parameters
        ----------
        xy : ndarray
            A numpy array of 2D points.
        ignore : bool, optional
           - When ``True``, ignore the existing bounds of the `Bbox`.
           - When ``False``, include the existing bounds of the `Bbox`.
           - When ``None``, use the last value passed to :meth:`ignore`.
        updatex, updatey : bool, default: True
            When ``True``, update the x/y values.
        r
Bbox.update_from_data_xyc
Bbox.x0c
Bbox.y0c
Bbox.x1c
Bbox.y1c
Bbox.p0c
Bbox.p1c
intervalr
Bbox.intervalxc
Bbox.intervalyc
Bbox.boundsc
        The minimum positive value in both directions within the Bbox.
        This is useful when dealing with logarithmic scales and other scales
        where negative bounds result in floating point errors, and will be used
        as the minimum extent instead of *p0*.
        
Bbox.minposc
        The minimum positive value in the *x*-direction within the Bbox.
        This is useful when dealing with logarithmic scales and other scales
        where negative bounds result in floating point errors, and will be used
        as the minimum *x*-extent instead of *x0*.
        r
minposx
Bbox.minposxc
        The minimum positive value in the *y*-direction within the Bbox.
        This is useful when dealing with logarithmic scales and other scales
        where negative bounds result in floating point errors, and will be used
        as the minimum *y*-extent instead of *y0*.
        r
minposy
Bbox.minposyc
        Get the points of the bounding box directly as a numpy array
        of the form: ``[[x0, y0], [x1, y1]]``.
        r
Bbox.get_pointsc
        Set the points of the bounding box directly from a numpy array
        of the form: ``[[x0, y0], [x1, y1]]``.  No error checking is
        performed, as this method is mainly for internal use.
        N)
set_points
Bbox.set_pointsc
        Set this bounding box from the "frozen" bounds of another `Bbox`.
        N)
set!
Bbox.setc
z/Return whether the bbox has changed since init.)
mutatedx
mutatedyr%
mutated)
Bbox.mutatedc
z4Return whether the x-limits have changed since init.rr
Bbox.mutatedxc
z4Return whether the y-limits have changed since init.rt
Bbox.mutatedy)
NTT)
NTT),r"
setterru
TransformedBboxz
    A `Bbox` that is automatically transformed by a given
    transform.  When either the child bounding box or transform
    changes, the bounds of this bbox will update accordingly.
    c
        Parameters
        ----------
        bbox : `Bbox`
        transform : `Transform`
        
'bbox' is not a bbox
z8The input and output dimensions of 'transform' must be 2N)
check_isinstance
	Transform
input_dims
output_dimsrD
_bbox
_transformrG
TransformedBbox.__init__r
)	Nrr
filledr}
TransformedBbox.get_pointsc
_get_pointsrl
Z	d	d
LockableBboxz
    A `Bbox` where some elements may be locked at certain values.
    When the child bounding box changes, the bounds of this bbox will update
    accordingly with the exception of the locked elements.
    Nc
        Parameters
        ----------
        bbox : `Bbox`
            The child bounding box to wrap.
        x0 : float or None
            The locked value for x0, or None to leave unlocked.
        y0 : float or None
            The locked value for y0, or None to leave unlocked.
        x1 : float or None
            The locked value for x1, or None to leave unlocked.
        y1 : float or None
            The locked value for y1, or None to leave unlocked.
        r
z)LockableBbox.__init__.<locals>.<listcomp>
maskr
_locked_points)	r
fpr 
LockableBbox.__init__r
wherer!
LockableBbox.get_pointsc
        float or None: The value used for the locked x0.
        rr
	locked_x0
LockableBbox.locked_x0c
datarU
        float or None: The value used for the locked y0.
        rt
	locked_y0
LockableBbox.locked_y0c
        float or None: The value used for the locked x1.
        rv
	locked_x1
LockableBbox.locked_x1c
        float or None: The value used for the locked y1.
        rx
	locked_y1
LockableBbox.locked_y1c
NNNN)
d d!
d"d#
d$d%
d&d'
d-d)d*
d+d,
    The base class of all `TransformNode` instances that
    actually perform a transformation.
    All non-affine transformations should be subclasses of this class.
    New affine transformations should be subclasses of `Affine2D`.
    Subclasses of this class should override the following members (at
    minimum):
    - :attr:`input_dims`
    - :attr:`output_dims`
    - :meth:`transform`
    - :meth:`inverted` (if an inverse exists)
    The following attributes may be overridden if the default is unsuitable:
    - :attr:`is_separable` (defaults to True for 1D -> 1D transforms, False
      otherwise)
    - :attr:`has_inverse` (defaults to True if :meth:`inverted` is overridden,
      False otherwise)
    If the transform needs to do something non-standard with
    `matplotlib.path.Path` objects, such as adding curves
    where there were once line segments, it should override:
    - :meth:`transform_path`
    NFc
is_separableN
z.Transform.__init_subclass__.<locals>.<genexpr>r
has_inverseNr,
inverted)	r
__mro__r
hasattrr/
clsr
__init_subclass__'
Transform.__init_subclass__c
        Compose two transforms together so that *self* is followed by *other*.
        ``A + B`` returns a transform ``C`` so that
        ``C.transform(x) == B.transform(A.transform(x))``.
        )
composite_transform_factory
NotImplementedr
__add__7
Transform.__add__c
        Return an iterator breaking down this transform stack from left to
        right recursively. If self == ((A, N), A) then the result will be an
        iterator which yields I : ((A, N), A), followed by A : (N, A),
        followed by (A, N) : (A), but not ((A, N), A) : I.
        This is equivalent to flattening the stack then yielding
        ``flat_stack[:i], flat_stack[i:]`` where i=0..(n-1).
        N
IdentityTransformr%
_iter_break_from_left_to_rightF
z(Transform._iter_break_from_left_to_rightc
        Return the number of transforms which have been chained
        together to form this Transform instance.
        .. note::
            For the special case of a Composite transform, the maximum depth
            of the two is returned.
        r
depthR
Transform.depthc
        Return whether the given transform is a sub-tree of this transform.
        This routine uses transform equality to identify sub-trees, therefore
        in many situations it is object id which will be used.
        For the case where the given transform represents the whole
        of this transform, returns True.
        FT)
sub_treer
contains_branch`
Transform.contains_branchc
        Return whether the given branch is a sub-tree of this transform on
        each separate dimension.
        A common use for this method is to identify if a transform is a blended
        transform containing an Axes' data transform. e.g.::
            x_isdata, y_isdata = trans.contains_branch_seperately(ax.transData)
        r/
zLcontains_branch_seperately only supports transforms with 2 output dimensions)
other_transformr
contains_branch_seperatelys
z$Transform.contains_branch_seperatelyc
        Compose *self* with the inverse of *other*, cancelling identical terms
        if any::
            # In general:
            A - B == A + B.inverted()
            # (but see note regarding frozen transforms below).
            # If A "ends with" B (i.e. A == A' + B for some A') we can cancel
            # out B:
            (A' + B) - B == A'
            # Likewise, if B "starts with" A (B = A + B'), we can cancel out A:
            A - (A + B') == B'.inverted() == B'^-1
        Cancellation (rather than naively returning ``A + B.inverted()``) is
        important for multiple reasons:
        - It avoids floating-point inaccuracies when computing the inverse of
          B: ``B - B`` is guaranteed to cancel out exactly (resulting in the
          identity transform), whereas ``B + B.inverted()`` may differ by a
          small epsilon.
        - ``B.inverted()`` always returns a frozen transform: if one computes
          ``A + B + B.inverted()`` and later mutates ``B``, then
          ``B.inverted()`` won't be updated and the last two terms won't cancel
          out anymore; on the other hand, ``A + B - B`` will always be equal to
          ``A`` even if ``B`` is mutated.
        zQThe shortcut cannot be computed since 'other' includes a non-invertible componentzpIt is not possible to compute transA - transB since transB cannot be inverted and there is no shortcut possible.N)
Z	remainderr;
__sub__
Transform.__sub__c
z9Array interface to get at this Transform's affine matrix.
get_affine
get_matrixrp
Transform.__array__c
        Apply this transformation on the given array of *values*.
        Parameters
        ----------
        values : array
            The input values as NumPy array of length :attr:`input_dims` or
            shape (N x :attr:`input_dims`).
        Returns
        -------
        array
            The output values as NumPy array of length :attr:`output_dims` or
            shape (N x :attr:`output_dims`), depending on the input.
        r
z6Input values must have shape (N x {dims}) or ({dims}).)
dimsN)	re
asanyarray
ndimr
transform_affine
transform_non_affiner
resr
Transform.transformc
        Apply only the affine part of this transformation on the
        given array of values.
        ``transform(values)`` is always equivalent to
        ``transform_affine(transform_non_affine(values))``.
        In non-affine transformations, this is generally a no-op.  In
        affine transformations, this is equivalent to
        ``transform(values)``.
        Parameters
        ----------
        values : array
            The input values as NumPy array of length :attr:`input_dims` or
            shape (N x :attr:`input_dims`).
        Returns
        -------
        array
            The output values as NumPy array of length :attr:`output_dims` or
            shape (N x :attr:`output_dims`), depending on the input.
        
Transform.transform_affinec
        Apply only the non-affine part of this transformation.
        ``transform(values)`` is always equivalent to
        ``transform_affine(transform_non_affine(values))``.
        In non-affine transformations, this is generally equivalent to
        ``transform(values)``.  In affine transformations, this is
        always a no-op.
        Parameters
        ----------
        values : array
            The input values as NumPy array of length :attr:`input_dims` or
            shape (N x :attr:`input_dims`).
        Returns
        -------
        array
            The output values as NumPy array of length :attr:`output_dims` or
            shape (N x :attr:`output_dims`), depending on the input.
        r
Transform.transform_non_affinec
        Transform the given bounding box.
        For smarter transforms including caching (a common requirement in
        Matplotlib), see `TransformedBbox`.
        )
transform_bbox
Transform.transform_bboxc
z&Get the affine part of this transform.r7
Transform.get_affinec
z5Get the matrix for the affine part of this transform.r?
Transform.get_matrixc
        Return a transformed point.
        This function is only kept for backcompatibility; the more general
        `.transform` method is capable of transforming both a list of points
        and a single point.
        The point is given as a sequence of length :attr:`input_dims`.
        The transformed point is returned as a sequence of length
        :attr:`output_dims`.
        z/The length of 'point' must be 'self.input_dims')
pointr
transform_point$
Transform.transform_pointc
        Apply the transform to `.Path` *path*, returning a new `.Path`.
        In some cases, this transform may insert curves into the path
        that began as line segments.
        )
transform_path_affine
transform_path_non_affine
transform_path4
Transform.transform_pathc
        Apply the affine part of this transform to `.Path` *path*, returning a
        new `.Path`.
        ``transform_path(path)`` is equivalent to
        ``transform_path_affine(transform_path_non_affine(values))``.
        )
Transform.transform_path_affinec
        Apply the non-affine part of this transform to `.Path` *path*,
        returning a new `.Path`.
        ``transform_path(path)`` is equivalent to
        ``transform_path_affine(transform_path_non_affine(values))``.
        )
_fast_from_codes_and_verts
codes)
z#Transform.transform_path_non_affine
}	|	S
        Transform a set of angles anchored at specific locations.
        Parameters
        ----------
        angles : (N,) array-like
            The angles to transform.
        pts : (N, 2) array-like
            The points where the angles are anchored.
        radians : bool, default: False
            Whether *angles* are radians or degrees.
        pushoff : float
            For each point in *pts* and angle in *angles*, the transformed
            angle is computed by transforming a segment of length *pushoff*
            starting at that point and making that angle relative to the
            horizontal axis, and measuring the angle between the horizontal
            axis and the transformed segment.
        Returns
        -------
        (N,) array
        r/
Only defined in 2D)
anglesz'There must be as many 'angles' as 'pts'Nr
check_shaper
deg2radr
sinr
arctan2Z
rad2deg)
pushoffZ
pts2Z
tptsZ
tpts2
transform_anglesR
Transform.transform_anglesc
        Return the corresponding inverse transformation.
        It holds ``x == self.inverted().transform(self.transform(x))``.
        The return value of this method should be treated as
        temporary.  An update to *self* does not cause a corresponding
        update to its inverted copy.
        Nr
Transform.inverted)
TransformWrappera
    A helper class that holds a single child transform and acts
    equivalently to it.
    This is useful if a node of the transform tree must be replaced at
    run time with a transform of a different type.  This class allows
    that replacement to correctly trigger invalidation.
    `TransformWrapper` instances must have the same input and output dimensions
    during their entire lifetime, so the child transform may only be replaced
    with another child transform of the same dimensions.
    Tc
        *child*: A `Transform` instance.  This child may later
        be replaced with :meth:`set`.
        )
_initrG
TransformWrapper.__init__c
_setr2
TransformWrapper._initc
_child
__eq__r
TransformWrapper.__eq__r[
TransformWrapper.frozenc
TransformWrapper._setc
        Replace the current child of this transform with another one.
        The new child must have the same number of input and output
        dimensions as the current child.
        z[The new child must have the same number of input and output dimensions as the current childr
TransformWrapper.setc
TransformWrapper.<lambda>c
AffineBasezS
    The base class of all affine transformations of any number of dimensions.
    Tc
	_invertedrp
AffineBase.__init__c
AffineBase.__array__c
AffineBase.__eq__c
AffineBase.transformc
Nz.Affine subclasses should override this method.r
AffineBase.transform_affinec
AffineBase.transform_non_affinec
AffineBase.transform_pathc
_interpolation_stepsrM
z AffineBase.transform_path_affinec
z$AffineBase.transform_path_non_affinec
AffineBase.get_affine)
Z	d	d
Affine2DBasea
    The base class of all 2D affine transformations.
    2D affine transformations are performed using a 3x3 numpy array::
        a c e
        b d f
        0 0 1
    This class provides the read-only interface.  For a mutable 2D
    affine transformation, use `Affine2D`.
    Subclasses of this class will generally only need to override a
    constructor and :meth:`get_matrix` that generates a custom 3x3 matrix.
    r/
Affine2DBase.frozenc
mtxr
Affine2DBase.is_separablec
        Return the values of the matrix as an ``(a, b, c, d, e, f)`` tuple.
        Nr/
tupleZ
swapaxesZ
flatra
	to_values1
Affine2DBase.to_valuesc
getmask)
tpointsr
Affine2DBase.transform_affinec
A non-numpy array of type zE was passed in for transformation, which results in poor performance.)	r
ndarrayr
_transform_affiner
(%s)-1)
Affine2DBase.invertedN)
d(d!d"
d#d$
d%d&
    A mutable 2D affine transformation.
    Nc
        Initialize an Affine transform from a 3x3 numpy float array::
          a c e
          b d f
          0 0 1
        If *matrix* is None, initialize with the identity transform.
        Nr
_mtxrC
matrixr(
Affine2D.__init__rf
Affine2D().scale(z
, r 
diagrj
	_base_strr%
Affine2D.__str__c
        Create a new Affine2D instance from the given values::
          a c e
          b d f
          0 0 1
        .
        r
from_valuesx
Affine2D.from_valuesc
        Get the underlying transformation matrix as a 3x3 numpy array::
          a c e
          b d f
          0 0 1
        .
        Nr
Affine2D.get_matrixc
        Set the underlying transformation matrix from a 3x3 numpy array::
          a c e
          b d f
          0 0 1
        .
        N
set_matrix
Affine2D.set_matrixc
        Set this transformation from the frozen copy of another
        `Affine2DBase` object.
        )
Affine2D.setz
3.6z
Affine2D())
alternativec
        Return a new `Affine2D` object that is the identity transform.
        Unless this transform will be mutated later on, consider using
        the faster `IdentityTransform` class instead.
        )
identity
Affine2D.identityc
        Reset the underlying matrix to the identity transform.
        )
clear
Affine2D.clearc
        Add a rotation (in radians) to this transform in place.
        Returns *self*, so this method can easily be chained with more
        calls to :meth:`rotate`, :meth:`rotate_deg`, :meth:`translate`
        and :meth:`scale`.
        rr
mathrS
tolistrU
thetar
yyru
Affine2D.rotatec
        Add a rotation (in degrees) to this transform in place.
        Returns *self*, so this method can easily be chained with more
        calls to :meth:`rotate`, :meth:`rotate_deg`, :meth:`translate`
        and :meth:`scale`.
        )
degreesr
rotate_deg
Affine2D.rotate_degc
        Add a rotation (in radians) around the point (x, y) in place.
        Returns *self*, so this method can easily be chained with more
        calls to :meth:`rotate`, :meth:`rotate_deg`, :meth:`translate`
        and :meth:`scale`.
        )
	translater
rotate_around
Affine2D.rotate_aroundc
        Add a rotation (in degrees) around the point (x, y) in place.
        Returns *self*, so this method can easily be chained with more
        calls to :meth:`rotate`, :meth:`rotate_deg`, :meth:`translate`
        and :meth:`scale`.
        )
rotate_deg_around
Affine2D.rotate_deg_aroundc
        Add a translation in place.
        Returns *self*, so this method can easily be chained with more
        calls to :meth:`rotate`, :meth:`rotate_deg`, :meth:`translate`
        and :meth:`scale`.
        rp
Affine2D.translatec
        Add a scale in place.
        If *sy* is None, the same scale is applied in both the *x*- and
        *y*-directions.
        Returns *self*, so this method can easily be chained with more
        calls to :meth:`rotate`, :meth:`rotate_deg`, :meth:`translate`
        and :meth:`scale`.
        Nrr
scale
Affine2D.scalec
        Add a skew in place.
        *xShear* and *yShear* are the shear angles along the *x*- and
        *y*-axes, respectively, in radians.
        Returns *self*, so this method can easily be chained with more
        calls to :meth:`rotate`, :meth:`rotate_deg`, :meth:`translate`
        and :meth:`scale`.
        rr
tanrf
xShear
yShear
ryrb
skew
Affine2D.skewc
        Add a skew in place.
        *xShear* and *yShear* are the shear angles along the *x*- and
        *y*-axes, respectively, in degrees.
        Returns *self*, so this method can easily be chained with more
        calls to :meth:`rotate`, :meth:`rotate_deg`, :meth:`translate`
        and :meth:`scale`.
        )
skew_deg4
Affine2D.skew_deg)
deprecatedrn
    A special class that does one thing, the identity transform, in a
    fast way.
    rh
IdentityTransform.frozenc
IdentityTransform.get_matrixc
IdentityTransform.transformc
z"IdentityTransform.transform_affinec
z&IdentityTransform.transform_non_affinec
z IdentityTransform.transform_pathc
z'IdentityTransform.transform_path_affinec
z+IdentityTransform.transform_path_non_affinec
IdentityTransform.get_affinec
IdentityTransform.invertedN)
_BlendedMixinzCCommon methods for `BlendedGenericTransform` and `BlendedAffine2D`.c
BlendedAffine2D
BlendedGenericTransform
_yr5
_BlendedMixin.__eq__c
z(_BlendedMixin.contains_branch_seperatelyr
    A "blended" transform uses one transform for the *x*-direction, and
    another transform for the *y*-direction.
    This "generic" version can handle any given child transform in the
    *x*- and *y*-directions.
    r/
        Create a new "blended" transform using *x_transform* to transform the
        *x*-axis and *y_transform* to transform the *y*-axis.
        You will generally not call this constructor directly but use the
        `blended_transform_factory` function instead, which can determine
        automatically which kind of blended transform to create.
        N)
_affine)
x_transform
y_transformr(
z BlendedGenericTransform.__init__c
BlendedGenericTransform.depthc
z'BlendedGenericTransform.contains_branchc
z BlendedGenericTransform.<lambda>c
blended_transform_factoryr
BlendedGenericTransform.frozenc
concatenate)
x_pointsZ
y_pointsr
z,BlendedGenericTransform.transform_non_affinec
z BlendedGenericTransform.invertedc
)	r2
x_mtx
y_mtxrb
z"BlendedGenericTransform.get_affineN)
    A "blended" transform uses one transform for the *x*-direction, and
    another transform for the *y*-direction.
    This version is an optimization for the case where both child
    transforms are of type `Affine2DBase`.
    Tc
        Create a new "blended" transform using *x_transform* to transform the
        *x*-axis and *y_transform* to transform the *y*-axis.
        Both *x_transform* and *y_transform* must be 2D affine transforms.
        You will generally not call this constructor directly but use the
        `blended_transform_factory` function instead, which can determine
        automatically which kind of blended transform to create.
        zABoth *x_transform* and *y_transform* must be 2D affine transformsN)
is_correctr
BlendedAffine2D.__init__c
BlendedAffine2D.get_matrixN)
    Create a new "blended" transform using *x_transform* to transform
    the *x*-axis and *y_transform* to transform the *y*-axis.
    A faster version of the blended transform is returned for the case
    where both child transforms are affine.
    )
CompositeGenericTransformz
    A composite transform formed by applying transform *a* then
    transform *b*.
    This "generic" version can handle any two arbitrary
    transformations.
    Tc
        Create a new composite transform that is the result of
        applying transform *a* then transform *b*.
        You will generally not call this constructor directly but write ``a +
        b`` instead, which will automatically choose the best kind of composite
        transform instance to create.
        
HThe output dimension of 'a' must be equal to the input dimensions of 'b'N)
_brG
z"CompositeGenericTransform.__init__c
z CompositeGenericTransform.frozenc
z.CompositeGenericTransform._invalidate_internalc
CompositeAffine2Dr
z CompositeGenericTransform.__eq__c
left
rightr
z8CompositeGenericTransform._iter_break_from_left_to_rightc
z"CompositeGenericTransform.<lambda>c
z*CompositeGenericTransform.transform_affinec
z.CompositeGenericTransform.transform_non_affinec
z3CompositeGenericTransform.transform_path_non_affinec
dotrA
z$CompositeGenericTransform.get_affinec
z"CompositeGenericTransform.inverted)
    A composite transform formed by applying transform *a* then transform *b*.
    This version is an optimization that handles the case where both *a*
    and *b* are 2D affines.
    c
        Create a new composite transform that is the result of
        applying `Affine2DBase` *a* then `Affine2DBase` *b*.
        You will generally not call this constructor directly but write ``a +
        b`` instead, which will automatically choose the best kind of composite
        transform instance to create.
        z%'a' and 'b' must be affine transformsr
CompositeAffine2D.__init__c
CompositeAffine2D.depthc
z0CompositeAffine2D._iter_break_from_left_to_rightr
CompositeAffine2D.get_matrix)
    Create a new composite transform that is the result of applying
    transform a then transform b.
    Shortcut versions of the blended transform are provided for the
    case where both child transforms are affine, or one or the other
    is the identity transform.
    Composite transforms may also be created using the '+' operator,
    e.g.::
      c = a + b
    )
BboxTransformzP
    `BboxTransform` linearly transforms points from one `Bbox` to another.
    Tc
        Create a new `BboxTransform` that linearly transforms
        points from *boxin* to *boxout*.
        z!'boxin' and 'boxout' must be bboxN)	ra
_boxin
_boxoutrG
boxin
boxoutr(
BboxTransform.__init__r
rR|	d
z/Transforming from or to a singular bounding boxr
outl
outb
outw
outh
x_scale
y_scaler
BboxTransform.get_matrix
BboxTransformToz
    `BboxTransformTo` is a transformation that linearly transforms points from
    the unit bounding box to a given `Bbox`.
    Tc
        Create a new `BboxTransformTo` that linearly transforms
        points from the unit bounding box to *boxout*.
        z
'boxout' must be bboxN)
BboxTransformTo.__init__r
z(Transforming to a singular bounding box.r
BboxTransformTo.get_matrixr
BboxTransformToMaxOnlyz
    `BboxTransformTo` is a transformation that linearly transforms points from
    the unit bounding box to a given `Bbox` with a fixed upper left of (0, 0).
    c
z!BboxTransformToMaxOnly.get_matrixN)
BboxTransformFromzj
    `BboxTransformFrom` linearly transforms points from a given `Bbox` to the
    unit bounding box.
    Tc
'boxin' must be bbox)
BboxTransformFrom.__init__r
z*Transforming from a singular bounding box.r
BboxTransformFrom.get_matrixr
ScaledTranslationz|
    A transformation that translates by *xt* and *yt*, after *xt* and *yt*
    have been transformed by *scale_trans*.
    c
_scale_transrG
scale_transr(
ScaledTranslation.__init__r
ScaledTranslation.get_matrix
AffineDeltaTransformav
    A transform wrapper for transforming displacements between pairs of points.
    This class is intended to be used to transform displacements ("position
    deltas") between pairs of points (e.g., as the ``offset_transform``
    of `.Collection`\s): given a transform ``t`` such that ``t =
    AffineDeltaTransform(t) + offset``, ``AffineDeltaTransform``
    satisfies ``AffineDeltaTransform(a - b) == AffineDeltaTransform(a) -
    AffineDeltaTransform(b)``.
    This is implemented by forcing the offset components of the transform
    matrix to zero.
    This class is experimental as of 3.3, and the API may change.
    c
_base_transform)
AffineDeltaTransform.__init__r
AffineDeltaTransform.get_matrixr
TransformedPathaa
    A `TransformedPath` caches a non-affine transformed copy of the
    `~.path.Path`.  This cached copy is automatically updated when the
    non-affine part of the transform changes.
    .. note::
        Paths are considered immutable by this class. Any update to the
        path's vertices/codes will not trigger a transform recomputation.
    c
        Parameters
        ----------
        path : `~.path.Path`
        transform : `Transform`
        r
_pathr
_transformed_path
_transformed_points)
TransformedPath.__init__c
_revalidate
TransformedPath._revalidatec
        Return a copy of the child path, with the non-affine part of
        the transform already applied, along with the affine part of
        the path necessary to complete the transformation.  Unlike
        :meth:`get_transformed_path_and_affine`, no interpolation will
        be performed.
        )
!get_transformed_points_and_affine
z1TransformedPath.get_transformed_points_and_affinec
        Return a copy of the child path, with the non-affine part of
        the transform already applied, along with the affine part of
        the path necessary to complete the transformation.
        )
get_transformed_path_and_affine
z/TransformedPath.get_transformed_path_and_affinec
        Return a fully-transformed copy of the child path.
        )
get_fully_transformed_path
z*TransformedPath.get_fully_transformed_pathc
TransformedPath.get_affine)
TransformedPatchPathz
    A `TransformedPatchPath` caches a non-affine transformed copy of the
    `~.patches.Patch`. This cached copy is automatically updated when the
    non-affine part of the transform or the patch changes.
    c
        Parameters
        ----------
        patch : `~.patches.Patch`
        N)
get_pathZ
get_transform
_patch)
patchrK
TransformedPatchPath.__init__c
patch_pathrK
z TransformedPatchPath._revalidate)
MbP?
    Modify the endpoints of a range as needed to avoid singularities.
    Parameters
    ----------
    vmin, vmax : float
        The initial endpoints.
    expander : float, default: 0.001
        Fractional amount by which *vmin* and *vmax* are expanded if
        the original interval is too small, based on *tiny*.
    tiny : float, default: 1e-15
        Threshold for the ratio of the interval to the maximum absolute
        value of its endpoints.  If the interval is smaller than
        this, it will be expanded.  This value should be around
        1e-15 or larger; otherwise the interval will be approaching
        the double precision resolution limit.
    increasing : bool, default: True
        If True, swap *vmin*, *vmax* if *vmin* > *vmax*.
    Returns
    -------
    vmin, vmax : float
        Endpoints, expanded and/or swapped if necessary.
        If either input is inf or NaN, or if both inputs are 0 or very
        close to zero, it returns -*expander*, *expander*.
    FTg
isfinite
mapr
absZ
finfo
tiny)
vminZ
vmaxZ
expanderr
increasingZ
swappedZ
maxabsvaluer
nonsingular
    Check, inclusively, whether an interval includes a given value.
    Parameters
    ----------
    interval : (float, float)
        The endpoints of the interval.
    val : float
        Value to check is within interval.
    Returns
    -------
    bool
        Whether *val* is within the *interval*.
    r
interval_containsE
    Check, inclusively, whether an interval includes a given value, with the
    interval expanded by a small tolerance to admit floating point errors.
    Parameters
    ----------
    interval : (float, float)
        The endpoints of the interval.
    val : float
        Value to check is within interval.
    rtol : float, default: 1e-10
        Relative tolerance slippage allowed outside of the interval.
        For an interval ``[a, b]``, values
        ``a - rtol * (b - a) <= val <= b + rtol * (b - a)`` are considered
        inside the interval.
    Returns
    -------
    bool
        Whether *val* is within the *interval* (with tolerance).
    r
rtolr
_interval_contains_close[
    Check, excluding endpoints, whether an interval includes a given value.
    Parameters
    ----------
    interval : (float, float)
        The endpoints of the interval.
    val : float
        Value to check is within interval.
    Returns
    -------
    bool
        Whether *val* is within the *interval*.
    r
interval_contains_openx
inchesc
    Return a new transform with an added offset.
    Parameters
    ----------
    trans : `Transform` subclass
        Any transform, to which offset will be applied.
    fig : `~matplotlib.figure.Figure`, default: None
        Current figure. It can be None if *units* are 'dots'.
    x, y : float, default: 0.0
        The offset to apply.
    units : {'inches', 'points', 'dots'}, default: 'inches'
        Units of the offset.
    Returns
    -------
    `Transform` subclass
        Transform with applied offset.
    
dotsNz3For units of inches or points a fig kwarg is neededrk
units)
check_in_listr
dpi_scale_trans)
transZ
figr
offset_copy
)3r_
numpyre
numpy.linalgr
matplotlibr
matplotlib._pathr
<module>
