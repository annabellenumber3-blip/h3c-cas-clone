m	Z	m
_api
_docstring)
	_AxesBase
_TransformedBoundsLocator)
Axisc
SecondaryAxisz4
    General class to hold a Secondary_X/Yaxis.
    c
        See `.secondary_xaxis` and `.secondary_yaxis` for the doc string.
        While there is no need for this to be private, it should really be
        called by those higher level functions.
        F
bottom
left
right
noneTg
_functions
_parent
_orientation
_ticks_set
super
__init__Z
figure
xaxis
_axis
_locstringsZ
_otherstrings
yaxis
_parentscale
set_location
set_functions
set_major_locator
mtickerZ
NullLocator
set_ticks_position
spines
set_visible
_pos
set_alignment)
self
parentZ
orientation
location
	functions
kwargsZ	otheraxis
	__class__
E/usr/lib64/python3.9/site-packages/matplotlib/axes/_secondary_axes.pyr
SecondaryAxis.__init__c
        Set if axes spine and labels are drawn at top or bottom (or left/right)
        of the axes.
        Parameters
        ----------
        align : str
            either 'top' or 'bottom' for orientation='x' or
            'left' or 'right' for orientation='y' axis.
        )
alignr
check_in_listr
set_label_position)
SecondaryAxis.set_alignmentc
        Set the vertical or horizontal location of the axes in
        parent-normalized coordinates.
        Parameters
        ----------
        location : {'top', 'bottom', 'left', 'right'} or float
            The position to put the secondary axis.  Strings can be 'top' or
            'bottom' for orientation='x' and 'right' or 'left' for
            orientation='y'. A float indicates the relative position on the
            parent axes to put the new axes, 0.0 being the bottom (or left)
            and 1.0 being the top (or right).
        )
location must be r
, or a float, not r
isinstance
strr$
ValueErrorr
_locr
set_axes_locatorr
Z	transAxes)
boundsr-
SecondaryAxis.set_locationNc
	_set_limsr
apply_aspect)
positionr+
SecondaryAxis.apply_aspectF)
minorc
	set_ticksZ
staler
ticks
labelsr6
retr-
SecondaryAxis.set_ticksc
        Set how the secondary axis converts limits from the parent axes.
        Parameters
        ----------
        functions : 2-tuple of func, or `Transform` with an inverse.
            Transform between the parent axis values and the secondary axis
            values.
            If supplied as a 2-tuple of functions, the first function is
            the forward transform function and the second is the inverse
            transform.
            If a transform is supplied, then the transform must have an
            inverse.
        
<lambda>
z-SecondaryAxis.set_functions.<locals>.<lambda>z
functions argument of secondary axes must be a two-tuple of callable functions with the first function being the transform and the second being the inverse)
tuple
callabler
_set_scale)
SecondaryAxis.set_functionsc
        Draw the secondary axes.
        Consults the parent axes for its limits and converts them
        using the converter specified by
        `~.axes._secondary_axes.set_functions` (or *functions*
        parameter when axes initialized.)
        N)
draw
argsr*
SecondaryAxis.drawc
)	z3
        Check if parent has set its scale
        r
logZ
functionlogZ
functionr
Z	get_scaleZ
set_xscaler
set_yscaler
get_ticklocsr
FixedLocator)
pscaleZ	set_scaleZ
defscaler8
SecondaryAxis._set_scalec
        Set the limits based on parent limits and the convert method
        between the parent and this secondary axes.
        r
get_xlimZ
set_xlimZ
get_ylimZ
set_ylimr
array)
limsZ
set_lim
orderZ
neworderr-
SecondaryAxis._set_limsc
        Secondary axes cannot set the aspect ratio, so calling this just
        sets a warning.
        z)Secondary axes can't set the aspect ratioN)
warn_externalrC
set_aspect
SecondaryAxis.set_aspectc
        Change the color of the secondary axes and all decorators.
        Parameters
        ----------
        color : color
        r
axisZ
colorsr
tick_paramsr"
	set_colorr
labelr
colorr-
SecondaryAxis.set_color)
__name__
__module__
__qualname__
__doc__r
copyr
__classcell__r-
Warnings
--------
This method is experimental as of 3.1, and the API may change.
Parameters
----------
location : {'top', 'bottom', 'left', 'right'} or float
    The position to put the secondary axis.  Strings can be 'top' or
    'bottom' for orientation='x' and 'right' or 'left' for
    orientation='y'. A float indicates the relative position on the
    parent axes to put the new axes, 0.0 being the bottom (or left)
    and 1.0 being the top (or right).
functions : 2-tuple of func, or Transform with an inverse
    If a 2-tuple of functions, the user specifies the transform
    function and its inverse.  i.e.
    ``functions=(lambda x: 2 / x, lambda x: 2 / x)`` would be an
    reciprocal transform with a factor of 2. Both functions must accept
    numpy arrays as input.
    The user can also directly supply a subclass of
    `.transforms.Transform` so long as it has an inverse.
    See :doc:`/gallery/subplots_axes_and_figures/secondary_axis`
    for examples of making these conversions.
Returns
-------
ax : axes._secondary_axes.SecondaryAxis
Other Parameters
----------------
**kwargs : `~matplotlib.axes.Axes` properties.
    Other miscellaneous axes parameters.
_secax_docstring)
numpyrF
matplotlibr
matplotlib.tickerZ
tickerr 
matplotlib.axes._baser
matplotlib.axisr
interpd
updater-
<module>
