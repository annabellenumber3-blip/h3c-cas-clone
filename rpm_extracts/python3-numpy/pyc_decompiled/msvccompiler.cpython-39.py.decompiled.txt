MSVCCompiler
platform_bitsc
Concatenate two environment paths avoiding repeats.
    Here `old` is the environment string before the base class initialize
    function is called and `new` is the string after the call. The new string
    will be a fixed string if it is not obtained from the current environment,
    or the same as the old string if obtained from the same environment. The aim
    here is not to append the new string if it is already contained in the old
    string so as to limit the growth of the environment string.
    Parameters
    ----------
    old : string
        Previous environment string.
    new : string
        New environment string.
    Returns
    -------
    ret : string
        Updated environment string.
    
join)
B/usr/lib64/python3.9/site-packages/numpy/distutils/msvccompiler.py
_merge
_MSVCCompiler
__init__)
self
verboseZ
dry_run
forcer	
MSVCCompiler.__init__c
include
/arch:SSE2)	
getenvr
initializer
environr
compile_optionsZ
compile_options_debug)
environ_libZ
environ_includer	
MSVCCompiler.initializeN)
__name__
__module__
__qualname__r
distutils.msvccompilerr
system_infor
<module>
