A module to test whether doctest recognizes some 2.2 features,
like static and class methods.
>>> print('yup')  # 1
We include some (random) encoded (utf-8) text in the text surrounding
the example.  It should be ignored:
Cannot test docstrings with -O2c
Class C.
    >>> print(C())  # 2
    42
    We include some (random) encoded (utf-8) text in the text surrounding
    the example.  It should be ignored:
        
    c
z;C.__init__.
        >>> print(C()) # 3
        42
        N
selfr
*/usr/lib64/python3.9/test/test_doctest2.py
__init__!
C.__init__c
        >>> print(C()) # 4
        42
        Z
__str__(
z	C.__str__c
C.DzJA nested D class.
        >>> print("In D!")   # 5
        In D!
        c
            >>> print(3) # 6
            3
            Nr
nested6
C.D.nestedN)
__name__
__module__
__qualname__
__doc__r
        >>> c = C()    # 7
        >>> c.x = 12   # 8
        >>> print(c.x)  # 9
        -12
        
getx<
C.getxc
        >>> c = C()     # 10
        >>> c.x = 12    # 11
        >>> print(c.x)   # 12
        -12
        Nr
valuer
setxE
C.setxzi        >>> c = C()    # 13
        >>> c.x = 12   # 14
        >>> print(c.x)  # 15
        -12
        )
docc
        A static method.
        >>> print(C.statm())    # 16
        666
        >>> print(C().statm())  # 17
        666
        i
statmU
C.statmc
        A class method.
        >>> print(C.clsm(22))    # 18
        22
        >>> print(C().clsm(23))  # 19
        23
        r
valr
clsma
C.clsmN)
objectr
property
staticmethodr
classmethodr
Testc
%d of %d doctests failedz expected %d tests to run, not %d)
doctest
sysZ
testmod
modulesr
fail)
EXPECTED
test_testmodo
Test.test_testmodN)
__main__)
unittest
flags
optimizeZ
SkipTestr
TestCaser
mainr
<module>
