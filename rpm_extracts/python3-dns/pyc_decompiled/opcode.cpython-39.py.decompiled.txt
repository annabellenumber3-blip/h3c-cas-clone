DNS Opcodes.
Opcoder
clsr
./usr/lib/python3.9/site-packages/dns/opcode.py
_maximum#
Opcode._maximumc
UnknownOpcoder	
_unknown_exception_class'
Opcode._unknown_exception_classN)
__name__
__module__
__qualname__
QUERY
IQUERY
STATUS
NOTIFY
UPDATE
classmethodr
An DNS opcode is unknown.N)
__doc__r
Convert text into an opcode.
    *text*, a ``str``, the textual opcode
    Raises ``dns.opcode.UnknownOpcode`` if the opcode is unknown.
    Returns an ``int``.
    )
	from_text)
textr
zpExtract an opcode from DNS message flags.
    *flags*, an ``int``, the DNS flags.
    Returns an ``int``.
    
flagsr
from_flags=
Convert an opcode to a value suitable for ORing into DNS message
    flags.
    *value*, an ``int``, the DNS opcode value.
    Returns an ``int``.
    r
valuer
to_flagsH
Convert an opcode to text.
    *value*, an ``int`` the opcode value,
    Raises ``dns.opcode.UnknownOpcode`` if the opcode is unknown.
    Returns a ``str``.
    )
to_textr 
zmIs the opcode in flags UPDATE?
    *flags*, an ``int``, the DNS message flags.
    Returns a ``bool``.
    )
	is_updatea
dns.enumZ
dnsZ
dns.exception
enum
IntEnumr
	exceptionZ
DNSExceptionr
<module>
