DNS Versioned Zones.
UseTransactionz-To alter a versioned zone, use a transaction.N)
__name__
__module__
__qualname__
__doc__
1/usr/lib/python3.9/site-packages/dns/versioned.pyr
	Z	d-d
d.d	d
d!d"
d#d$
d%d&
d'd(
d)d*
d+d,
Zone)
	_versionsZ
_versions_lock
_write_txn
_write_waiters
_write_event
_pruning_policy
_readersTNc
Initialize a versioned zone object.
        *origin* is the origin of the zone.  It may be a ``dns.name.Name``,
        a ``str``, or ``None``.  If ``None``, then the zone's origin will
        be set by the first ``$ORIGIN`` line in a zone file.
        *rdclass*, an ``int``, the zone's rdata class; the default is class IN.
        *relativize*, a ``bool``, determine's whether domain names are
        relativized to the zone's origin.  The default is ``True``.
        *pruning policy*, a function taking a `Version` and returning
        a `bool`, or `None`.  Should the version be pruned?  If `None`,
        the default policy, which retains one version is used.
        NT)
replacement)
super
__init__
collections
dequer
_threadingZ
Lock
_version_lock
_default_pruning_policyr
setr
_commit_version_unlocked
WritableVersion)
self
origin
rdclass
relativizeZ
pruning_policy
	__class__r
Zone.__init__c
qPq8|
Nz!cannot specify both id and serialz
version not foundr
serial not found
ValueErrorr
reversedr
KeyErrorr
name
emptyr
nodes
get_rdatasetr
	rdatatypeZ
serial
Transactionr
add)	r
version
oname
txnr
readerI
Zone.readerFc
make_immutable)
Eventr
append
waitZ
_setup_version)
eventr
writerj
Zone.writerc
lenr
popleftr
!_maybe_wakeup_one_waiter_unlocked
z&Zone._maybe_wakeup_one_waiter_unlockedc
zoner0
Zone._default_pruning_policyc
.0r3
	<genexpr>
z0Zone._prune_versions_unlocked.<locals>.<genexpr>r!
minr
least_keptr
_prune_versions_unlocked
Zone._prune_versions_unlockedc
zYSet a pruning policy that retains up to the specified number
        of versions
        N
max versions must be at least 1c
policy
z%Zone.set_max_versions.<locals>.policyc
max_versionsr
set_pruning_policy)
set_max_versions
Zone.set_max_versionsc
Set the pruning policy for the zone.
        The *policy* function takes a `Version` and returns `True` if
        the version should be pruned, and `False` otherwise.  `None`
        may also be specified for policy, in which case the default policy
        is used.
        Pruning checking proceeds from the least version and the first
        time the function returns `False`, the checking stops.  I.e. the
        retained versions are always a consecutive sequence.
        N)
Zone.set_pruning_policyc
removerA
	_end_read
Zone._end_readc
_end_write_unlocked
Zone._end_write_unlockedc
_end_write
Zone._end_writec
Zone._commit_version_unlockedc
_commit_version
Zone._commit_versionc
_get_next_version_id
Zone._get_next_version_idc
	find_node)
creater
Zone.find_nodec
delete_node
Zone.delete_nodec
find_rdatasetr&
rdataset
ImmutableRdataset
rdtype
coversrR
Zone.find_rdatasetc
Zone.get_rdatasetc
delete_rdataset
Zone.delete_rdatasetc
replace_rdataset
Zone.replace_rdataset)
F) r
	__slots__
NodeZ
node_factoryr&
rdataclass
NONErU
__classcell__r
Z	threadingr
ImportErrorZ
dummy_threadingZ
dns.exceptionr&
dns.immutableZ
dns.nameZ
dns.rdataclassZ
dns.rdatatypeZ
dns.rdtypes.ANY.SOAZ
dns.zone
	exceptionZ
DNSExceptionr
VersionedNoder^
ImmutableVersionedNodeZ
ImmutableNodeZ
Versionr
ImmutableVersionr.
<module>
