m	Z	
ConfigParser)
Path)
Optional
PlatformDirsABCZ
linux)
getuid
returnc
should only be used on Linux)
RuntimeError
A/usr/lib/python3.9/site-packages/pip/_vendor/platformdirs/unix.pyr
UnixaD
    On Unix/Linux, we follow the
    `XDG Basedir Spec <https://specifications.freedesktop.org/basedir-spec/basedir-spec-latest.html>`_. The spec allows
    overriding directories with environment variables. The examples show are the default values, alongside the name of
    the environment variable that overrides them. Makes use of the
    `appname <platformdirs.api.PlatformDirsABC.appname>`,
    `version <platformdirs.api.PlatformDirsABC.version>`,
    `multipath <platformdirs.api.PlatformDirsABC.multipath>`,
    `opinion <platformdirs.api.PlatformDirsABC.opinion>`.
    r
        :return: data directory tied to the user, e.g. ``~/.local/share/$appname/$version`` or
         ``$XDG_DATA_HOME/$appname/$version``
        Z
XDG_DATA_HOME
~/.local/share
environ
strip
path
expanduser
_append_app_name_and_version
selfr
user_data_dir
Unix.user_data_dirc
        :return: data directories shared by users (if `multipath <platformdirs.api.PlatformDirsABC.multipath>` is
         enabled and ``XDG_DATA_DIR`` is set and a multi path the response is also a multi path separated by the OS
         path separator), e.g. ``/usr/local/share/$appname/$version`` or ``/usr/share/$appname/$version``
        Z
XDG_DATA_DIRSr
/usr/local/sharez
/usr/share)
pathsep
_with_multi_pathr
site_data_dir(
Unix.site_data_dir)
<listcomp>9
z)Unix._with_multi_path.<locals>.<listcomp>)
splitr
	multipath
join)
	path_listr
Unix._with_multi_pathc
        :return: config directory tied to the user, e.g. ``~/.config/$appname/$version`` or
         ``$XDG_CONFIG_HOME/$appname/$version``
        Z
XDG_CONFIG_HOMEr
z	~/.configr
user_config_dir<
Unix.user_config_dirc
        :return: config directories shared by users (if `multipath <platformdirs.api.PlatformDirsABC.multipath>`
         is enabled and ``XDG_DATA_DIR`` is set and a multi path the response is also a multi path separated by the OS
         path separator), e.g. ``/etc/xdg/$appname/$version``
        Z
XDG_CONFIG_DIRSr
/etc/xdg)
site_config_dirG
Unix.site_config_dirc
        :return: cache directory tied to the user, e.g. ``~/.cache/$appname/$version`` or
         ``~/$XDG_CACHE_HOME/$appname/$version``
        Z
XDG_CACHE_HOMEr
~/.cacher
user_cache_dirT
Unix.user_cache_dirc
        :return: state directory tied to the user, e.g. ``~/.local/state/$appname/$version`` or
         ``$XDG_STATE_HOME/$appname/$version``
        Z
XDG_STATE_HOMEr
~/.local/stater
user_state_dir_
Unix.user_state_dirc
        :return: log directory tied to the user, same as `user_data_dir` if not opinionated else ``log`` in it
        
log)
opinionr
user_log_dirj
Unix.user_log_dirc
        :return: documents directory tied to the user, e.g. ``~/Documents``
        Z
XDG_DOCUMENTS_DIRNr
~/Documents)
_get_user_dirs_folderr
documents_dirr
user_documents_dirt
Unix.user_documents_dirc
        :return: runtime directory tied to the user, e.g. ``/run/user/$(id -u)/$appname/$version`` or
         ``$XDG_RUNTIME_DIR/$appname/$version``
        Z
XDG_RUNTIME_DIRr
/run/user/)
user_runtime_dir
Unix.user_runtime_dirc
zd:return: data path shared by users. Only return first item, even if ``multipath`` is set to ``True``)
 _first_item_as_path_if_multipathr
site_data_path
Unix.site_data_pathc
zj:return: config path shared by the users. Only return first item, even if ``multipath`` is set to ``True``)
site_config_path
Unix.site_config_path)
	directoryr	
z%Unix._first_item_as_path_if_multipathN)
__name__
__module__
__qualname__
__doc__
property
strr
keyr	
zjReturn directory from user-dirs.dirs config file. See https://freedesktop.org/wiki/Software/xdg-user-dirs/z
user-dirs.dirsz
[top]
$HOME
existsr
openZ
read_string
readr
replacer
user_dirs_config_path
parser
streamr
sysZ
configparserr
pathlibr
typingr
apir
platform
startswithr
intr
__all__r
<module>
