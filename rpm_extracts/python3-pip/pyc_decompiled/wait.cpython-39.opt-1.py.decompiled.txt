d	e	
_utils)
RetryCallStatec
	wait_basez(Abstract base class for wait strategies.r
retry_state
returnc
selfr
=/usr/lib/python3.9/site-packages/pip/_vendor/tenacity/wait.py
__call__
wait_base.__call__
wait_combine)
otherr
__add__"
wait_base.__add__)
__radd__%
wait_base.__radd__N)
__name__
__module__
__qualname__
__doc__
abstractmethod
floatr
typing
Unionr
wait_fixedzCWait strategy that waits a fixed amount of time between each retry.N)
waitr
__init__/
wait_fixed.__init__r
wait_fixed.__call__)
	wait_nonez7Wait strategy that doesn't wait at all before retrying.N)
superr 
	__class__r	
wait_none.__init__)
__classcell__r	
wait_randomzAWait strategy that waits a random amount of time between min/max.r
maxr
wait_random_min
wait_random_max)
wait_random.__init__r
randomr+
wait_random.__call__)
intr
z#Combine several waiting strategies.N
strategiesr
wait_funcs
wait_combine.__init__r
	<genexpr>O
z(wait_combine.__call__.<locals>.<genexpr>)
sumr0
wait_combine.__call__
wait_chaina
Chain two or more waiting strategies.
    If all strategies are exhausted, the very last strategy is used
    thereafter.
    For example::
        @retry(wait=wait_chain(*[wait_fixed(1) for i in range(3)] +
                               [wait_fixed(2) for j in range(5)] +
                               [wait_fixed(5) for k in range(4)))
        def wait_chained():
            print("Wait 1s for 3 attempts, 2s for 5 attempts and 5s
                   thereafter.")
    Nr.
wait_chain.__init__r
attempt_number
lenr/
wait_func_noZ	wait_funcr	
wait_chain.__call__r8
e	d	
wait_incrementingz
Wait an incremental amount of time after each attempt.
    Starting at a starting value and incrementing by a value for each attempt
    (and restricting the upper limit to some maximum value).
    r
start
	incrementr)
wait_incrementing.__init__r
resultr	
wait_incrementing.__call__
MAX_WAITr
d	e	d
wait_exponentiala
Wait strategy that applies exponential backoff.
    It allows for a customized multiplier and an ability to restrict the
    upper and lower limits to some maximum and minimum value.
    The intervals are fixed (i.e. there is no jitter), so this strategy is
    suitable for balancing retries against latency when a required resource is
    unavailable for an unknown duration, but *not* suitable for resolving
    contention between multiple processes for a shared resource. Use
    wait_random_exponential for the latter case.
    r'
multiplierr)
exp_baser(
wait_exponential.__init__r
OverflowErrorr)
exprA
wait_exponential.__call__rB
wait_random_exponentiala
Random wait with exponentially widening window.
    An exponential backoff strategy used to mediate contention between multiple
    uncoordinated processes for a shared resource in distributed systems. This
    is the sense in which "exponential backoff" is meant in e.g. Ethernet
    networking, and corresponds to the "Full Jitter" algorithm described in
    this blog post:
    https://aws.amazon.com/blogs/architecture/exponential-backoff-and-jitter/
    Each retry occurs at a random time in a geometrically expanding interval.
    It allows for a custom multiplier and an ability to restrict the upper
    limit of the random interval to some maximum value.
    Example::
        wait_random_exponential(multiplier=0.5,  # initial window 0.5s
                                max=60)          # max 60s timeout
    When waiting for an unavailable resource to become available again, as
    opposed to trying to resolve contention for a shared resource, the
    wait_exponential strategy (which uses a fixed interval) may be preferable.
    r
uniform)
highr#
z wait_random_exponential.__call__)
pip._vendor.tenacityr
TYPE_CHECKINGr
ABCr
<module>
