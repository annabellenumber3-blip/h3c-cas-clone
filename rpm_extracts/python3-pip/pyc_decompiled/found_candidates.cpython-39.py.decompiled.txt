m	Z	m
Utilities to lazily create and visit candidates found.
Creating and visiting a candidate is a *very* costly operation. It involves
fetching, extracting, potentially building modules from source, and verifying
distribution metadata. It is therefore crucial for performance to keep
everything here lazy all the way down, so we only touch candidates that we
absolutely need, and not "download the world" when we only need one version of
something.
Sequence)
TYPE_CHECKING
Callable
Iterator
Optional
Tuple)
_BaseVersion
	Candidate)
infos
returnc
Iterator for ``FoundCandidates``.
    This iterator is used when the package is not already installed. Candidates
    from index come later in their normal ordering.
    N)
add)
versions_found
version
func
	candidate
X/usr/lib/python3.9/site-packages/pip/_internal/resolution/resolvelib/found_candidates.py
_iter_built%
	installedr
Iterator for ``FoundCandidates``.
    This iterator is used when the resolver prefers the already-installed
    candidate and NOT to upgrade. The installed candidate is therefore
    always yielded first, and candidates from index come later in their
    normal ordering, except skipped when the version is already installed.
    N)
_iter_built_with_prepended6
Iterator for ``FoundCandidates``.
    This iterator is used when the resolver prefers to upgrade an
    already-installed package. Candidates from index are returned in their
    normal ordering, except replaced when the version is already installed.
    The implementation iterates through and yields other candidates, inserting
    the installed candidate exactly once before we start yielding older or
    equivalent candidates, or after all other candidates if they are all newer.
    N)
_iter_built_with_insertedL
FoundCandidatesac
A lazy sequence to provide candidates to the resolver.
    The intended usage is to return this from `find_matches()` so the resolver
    can iterate through the sequence multiple times, but only access the index
    page when remote packages are actually needed. This improve performances
    when suitable candidates are already installed on disk.
    )
	get_infosr
prefers_installed
incompatible_idsc
_get_infos
_installed
_prefers_installed
_incompatible_ids)
selfr
__init__u
FoundCandidates.__init__)
indexr
don't do this
NotImplementedError)
__getitem__
FoundCandidates.__getitem__)
idr$
	<genexpr>
z+FoundCandidates.__iter__.<locals>.<genexpr>)
iteratorr
__iter__
FoundCandidates.__iter__c
__len__
FoundCandidates.__len__r
maxsizec
anyr/
__bool__
FoundCandidates.__bool__N)
__name__
__module__
__qualname__
__doc__r
IndexCandidateInfor
boolr
intr&
	functools
	lru_cacher7
collections.abcr
typingr
pip._vendor.packaging.versionr
baser
SequenceCandidater
<module>
