d	d	d	d
d!d"
Z e 
Z!d#d$
d%d&
d&e#
d'd(
d(e$
d)d*
d*e$
d+d,
d,e'
d-d.
d.e$
d/d0
d0e$
d1d2
d2e$
d3d4
d4e+
d5d6
d6e$
d7d8
d8e+
d9d:
d:e$
d;d<
d=d>
d>e0
d?d@
d@e0
Z2e1e2dA
Z3dBdC
dDdE
dEe5
dFdG
dGe6
dHdI
Z8dOdJdK
Z9dPdMdN
BOM_UTF8
	BOM_UTF16
BOM_UTF16_BE
BOM_UTF16_LE)
__version__)
utf_8N)
utf16_be
utf_16)
utf16_ler	
utf16z
utf-16r
	utf_16_bez
utf-16ber
	utf_16_lez
utf-16ler
utf8
utf-8)
BOM_LIST
lower)
encoding
-/usr/lib/python3.9/site-packages/configobj.py
match_utf8D
'%s'z
"%s"z
	'"z
"""%s"""z
'''%s''')
DEFAULT_INDENT_TYPE
DEFAULT_INTERPOLATION
ConfigObjError
NestingError
ParseError
DuplicateError
ConfigspecError
	ConfigObj
	SimpleVal
InterpolationError
InterpolationLoopError
MissingInterpolationOption
RepeatSectionError
ReloadError
UnreprError
UnknownType
flatten_errors
get_extra_values
configparserz
    
interpolation
raise_errors
list_values
create_empty
file_error
configspec
	stringify
indent_typer
default_encoding
unrepr
write_empty_valuesc
compiler
parse
getChildren)
getObj
__name__
__module__
__qualname__r
Builderc
	__class__r@
self
build
Builder.buildc
list
maprJ
build_List
Builder.build_Listc
valuerG
build_Const
Builder.build_Constc
iterrL
next)
build_Dict
Builder.build_Dictc
tuplerM
build_Tuple
Builder.build_Tuplec
None
TrueT
FalseFz
Undefined Name)
namer)
build_Name
Builder.build_Namec
Addg
float
	TypeErrorr)
isinstance
complex
real)
imagr
	build_Add
Builder.build_Addc
expr
getattr
attrname)
parentr
build_Getattr
Builder.build_Getattrc
build_UnarySub
Builder.build_UnarySubc
build_UnaryAdd
Builder.build_UnaryAddN)
astZ
literal_eval)
    This is the base class for all errors that ConfigObj raises.
    It is a subclass of SyntaxError.
    
line
line_number
SyntaxError
__init__)
messagero
ConfigObjError.__init__)
__doc__rq
    This error indicates a level of nesting that doesn't match.
    N
    This error indicates that a line is badly written.
    It is neither a valid ``key = value`` line,
    nor a valid section marker line.
    Nru
    A 'reload' operation failed.
    This exception is a subclass of ``IOError``.
    c
Nz#reload failed, filename is not set.)
IOErrorrq
ReloadError.__init__Nrs
    The keyword or section specified already exists.
    Nru
    An error occured whilst parsing a configspec.
    Nru
z,Base class for the two interpolation errors.Nru
z=Maximum interpolation depth exceeded in string interpolation.c
Nz*interpolation loop detected in value "%s".
optionr
InterpolationLoopError.__init__Nrs
    This error indicates additional sections in a section with a
    ``__many__`` (repeated) section.
    Nru
z0A value specified for interpolation was missing.c
Nz%missing option "%s" in interpolation.rx
msgr
z#MissingInterpolationOption.__init__Nrs
z An error parsing in unrepr mode.Nru
InterpolationEnginez
    A helper class to help perform string interpolation.
    This class is an abstract base class; its descendants perform
    the actual work.
    
%\(([^)]*)\)s
section)
InterpolationEngine.__init__c
The function that does the actual work.
            ``value``: the string we're trying to interpolate.
            ``section``: the section in which that string was found
            ``backtrail``: a dict to keep track of where we've been,
            to detect and prevent infinite recursion loops
            This is similar to a depth-first-search algorithm.
            r9
_KEYCRE
search
_parse_match
span
join
len)
keyrN
Z	backtrail
match
replacement
start
endZ
new_search_start
recursive_interpolaterH
z>InterpolationEngine.interpolate.<locals>.recursive_interpolate)
_cookier
interpolate/
InterpolationEngine.interpolatec
s8qr|
s^qr|
rjqr|
Helper function to fetch values from owning section.
        Returns a 2-tuple: the value, and the section where it was found.
        FN
DEFAULT)
mainr.
Sectionrf
save_interpZ
current_section
valr
_fetchd
InterpolationEngine._fetchc
Implementation-dependent helper function.
        Will be passed a match object corresponding to the interpolation
        key we just found (e.g., "%(foo)s" or "$foo"). Should look up that
        key in the appropriate config file section (using the ``_fetch()``
        helper function) and return a 3-tuple: (key, value, section)
        ``key`` is the name of the key we're looking for
        ``value`` is the value found for that key
        ``section`` is a reference to the section where it was found
        ``key`` and ``section`` should be None if no further
        interpolation should be performed on the resulting value
        (e.g., if we interpolated "$$" and returned "$").
        N)
NotImplementedError)
z InterpolationEngine._parse_matchN)
compiler
ConfigParserInterpolationz
Behaves like ConfigParser.r}
groupr
z&ConfigParserInterpolation._parse_matchN)	r@
TemplateInterpolationz
Behaves like string.Template.
        \$(?:
          (?P<escaped>\$)              |   # Two $ signs
          (?P<named>[_a-z][_a-z0-9]*)  |   # $name format
          {(?P<braced>[^}]*)}              # ${name} format
        )
        c
namedZ
bracedZ
escaped)
_delimiterr
z"TemplateInterpolation._parse_matchN)
IGNORECASE
VERBOSEr
templatec
__new__)
argsr
__newobj__
Z	dEd
d d!
d"d#
d$d%
d&d'
d(d)
d*d+
d,d-
d/d0
d1d2
d3d4
dHd6d7
d8d9
Z d:d;
Z!d<d=
Z"d>d?
Z#d@dA
Z$dBdC
    A dictionary-like object that represents a section in a config file.
    
    It does string interpolation if the 'interpolation' attribute
    of the 'main' object is set to True.
    
    Interpolation is tried first from this object, then from the 'DEFAULT'
    section of this object, next from the parent and its 'DEFAULT' section,
    and so on until the main object is reached.
    
    A Section will behave like an ordered dictionary - following the
    order of the ``scalars`` and ``sections`` attributes.
    You can use this to change the order of members.
    
    Iteration follows the order: scalars, then sections.
    c
dict
update
__dict__
stater
__setstate__
Section.__setstate__c
__reduce__
Section.__reduce__Nc
        * parent is the section above
        * depth is the depth level of this section
        * main is the main ConfigObj
        * indict is a dictionary to initialise the section with
        N)
depthrZ
_initialise
items)
indictrZ
entryrN
Section.__init__c
NF)	
scalars
sections
comments
inline_commentsr3
defaults
default_values
extra_values
_createdrw
Section._initialisec
NTF)	Z
_interpolation_engine
AttributeErrorr
interpolation_enginesr
enginerZ
class_r
_interpolate
Section._interpolatec
z+Fetch the item and do string interpolation.c
string_typesr
_check/
z#Section.__getitem__.<locals>._checkc
<listcomp>3
z'Section.__getitem__.<locals>.<listcomp>)	r
__getitem__r
newr
Section.__getitem__Fc
        Correctly set a value.
        
        Making dictionary values Section instances.
        (We have to special case 'Section' instances - which are also dicts)
        
        Keys must be strings.
        Values need only be strings (or lists of strings) if
        ``main.stringify`` is set.
        
        ``unrepr`` must be set when setting a value to a dictionary, without
        creating a new sub-section.
        z
The key "%s" is not a string.rm
Value is not a string "%s".N)
ValueErrorr
remover
appendr
__setitem__r
Z	new_depthr
Section.__setitem__c
z-Remove items from the sequence when deleting.N)
__delitem__r
Section.__delitem__c
z>A version of ``get`` that doesn't bypass string interpolation.N
KeyError
defaultr
Section.getc
        A version of update that uses our ``__setitem__``.
        Nr
Section.updatec
        'D.pop(k[,d]) -> v, remove specified key and return the corresponding value.
        If key is not found, d is returned if given, otherwise KeyError is raised'
        )
MISSING)
Section.popc
Pops the first (key,val)z": 'popitem(): dictionary is empty'r
sequencer
popitem
Section.popitemc
        A version of clear that also affects scalars/sections
        Also clears comments and configspec.
        
        Leaves other attributes alone :
            depth/main/parent are not affected
        N)	r
clearr
Section.clearc
z:A version of setdefault that sets sequence if appropriate.Nr
setdefault
Section.setdefaultc
z8D.items() -> list of D's (key, value) pairs, as 2-tuples)
zipr
valuesrw
Section.itemsc
D.keys() -> list of D's keys
keys
Section.keysc
z D.values() -> list of D's valuesc
z"Section.values.<locals>.<listcomp>r
Section.valuesc
z=D.iteritems() -> an iterator over the (key, value) items of D)
	iteritems
Section.iteritemsc
z.D.iterkeys() -> an iterator over the keys of D)
iterkeys
Section.iterkeysc
z2D.itervalues() -> an iterator over the values of D)
itervalues
Section.itervaluesc
x.__repr__() <==> repr(x)c
_getval
z!Section.__repr__.<locals>._getvalz
{%s}
%s: %s
reprr
z$Section.__repr__.<locals>.<listcomp>
__repr__
Section.__repr__z
x.__str__() <==> str(x)c
        Return a deepcopy of self as a dictionary.
        
        All members that are ``Section`` instances are recursively turned to
        ordinary dictionaries - by calling their ``dict`` method.
        
        >>> n = a.dict()
        >>> n == a
        1
        >>> n is a
        0
        )
newdictr
this_entryr
Section.dictc
        A recursive update - useful for merging config files.
        
        >>> a = '''[section1]
        ...     option1 = True
        ...     [[subsection]]
        ...     more_options = False
        ...     # end of file'''.splitlines()
        >>> b = '''# File is user.ini
        ...     [section1]
        ...     option1 = False
        ...     # end of file'''.splitlines()
        >>> c1 = ConfigObj(b)
        >>> c2 = ConfigObj(a)
        >>> c2.merge(c1)
        >>> c2
        ConfigObj({'section1': {'option1': 'False', 'subsection': {'more_options': 'False'}}})
        N)
merge)
Section.mergec
        Change a keyname to another, without changing position in sequence.
        
        Implemented so that transformations can be made on keys,
        as well as on values. (used by encode and decode)
        
        Also renames comments.
        z
Key "%s" not found.N)
indexr
insertr
oldkeyZ
newkey
the_list
posr
commZ
inline_commentr
rename,
Section.renameTc
        Walk every member and call a function on the keyword and value.
        
        Return a dictionary of the return values
        
        If the function raises an exception, raise the errror
        unless ``raise_errors=False``, in which case set the return value to
        ``False``.
        
        Any unrecognised keyword arguments you pass to walk, will be pased on
        to the function you pass in.
        
        Note: if ``call_on_sections`` is ``True`` then - on encountering a
        subsection, *first* the function is called for the *whole* subsection,
        and then recurses into it's members. This means your function must be
        able to handle strings, dictionaries and lists. This allows you
        to change the key of subsections as well as for ordinary members. The
        return value when called on the whole subsection has to be discarded.
        
        See  the encode and decode methods for examples, including functions.
        
        .. admonition:: caution
        
            You can use ``walk`` to transform the names of members of a section
            but you mustn't add or delete members.
        
        >>> config = '''[XXXXsection]
        ... XXXXkey = XXXXvalue'''.splitlines()
        >>> cfg = ConfigObj(config)
        >>> cfg
        ConfigObj({'XXXXsection': {'XXXXkey': 'XXXXvalue'}})
        >>> def transform(section, key):
        ...     val = section[key]
        ...     newkey = key.replace('XXXX', 'CLIENT1')
        ...     section.rename(key, newkey)
        ...     if isinstance(val, (tuple, list, dict)):
        ...         pass
        ...     else:
        ...         val = val.replace('XXXX', 'CLIENT1')
        ...         section[newkey] = val
        >>> cfg.walk(transform, call_on_sections=True)
        {'CLIENT1section': {'CLIENT1key': None}}
        >>> cfg
        ConfigObj({'CLIENT1section': {'CLIENT1key': 'CLIENT1value'}})
        F)
call_on_sections)
ranger
	Exceptionr
walk)	rH
functionr/
keywargs
outrS
Section.walkc
        Accepts a key as input. The corresponding value must be a string or
        the objects (``True`` or 1) or (``False`` or 0). We allow 0 and 1 to
        retain compatibility with Python 2.2.
        
        If the string is one of  ``True``, ``On``, ``Yes``, or ``1`` it returns 
        ``True``.
        
        If the string is one of  ``False``, ``Off``, ``No``, or ``0`` it returns 
        ``False``.
        
        ``as_bool`` is not case sensitive.
        
        Any other input will raise a ``ValueError``.
        
        >>> a = ConfigObj()
        >>> a['a'] = 'fish'
        >>> a.as_bool('a')
        Traceback (most recent call last):
        ValueError: Value "fish" is neither True nor False
        >>> a['b'] = 'True'
        >>> a.as_bool('b')
        1
        >>> a['b'] = 'off'
        >>> a.as_bool('b')
        0
        TFz$Value "%s" is neither True nor FalseN)
_boolsr
as_bool
Section.as_boolc
        A convenience method which coerces the specified value to an integer.
        
        If the value is an invalid literal for ``int``, a ``ValueError`` will
        be raised.
        
        >>> a = ConfigObj()
        >>> a['a'] = 'fish'
        >>> a.as_int('a')
        Traceback (most recent call last):
        ValueError: invalid literal for int() with base 10: 'fish'
        >>> a['b'] = '1'
        >>> a.as_int('b')
        1
        >>> a['b'] = '3.2'
        >>> a.as_int('b')
        Traceback (most recent call last):
        ValueError: invalid literal for int() with base 10: '3.2'
        )
intr
as_int
Section.as_intc
        A convenience method which coerces the specified value to a float.
        
        If the value is an invalid literal for ``float``, a ``ValueError`` will
        be raised.
        
        >>> a = ConfigObj()
        >>> a['a'] = 'fish'
        >>> a.as_float('a')  #doctest: +IGNORE_EXCEPTION_DETAIL
        Traceback (most recent call last):
        ValueError: invalid literal for float(): fish
        >>> a['b'] = '1'
        >>> a.as_float('b')
        1.0
        >>> a['b'] = '3.2'
        >>> a.as_float('b')  #doctest: +ELLIPSIS
        3.2...
        )
as_float
Section.as_floatc
        A convenience method which fetches the specified value, guaranteeing
        that it is a list.
        
        >>> a = ConfigObj()
        >>> a['a'] = 1
        >>> a.as_list('a')
        [1]
        >>> a['a'] = (1,)
        >>> a.as_list('a')
        [1]
        >>> a['a'] = [1]
        >>> a.as_list('a')
        [1]
        )
resultr
as_list
Section.as_listc
        Restore (and return) default value for the specified key.
        
        This method will only work for a ConfigObj that was created
        with a configspec and has been validated.
        
        If there is no default value for this key, ``KeyError`` is raised.
        )
restore_default
Section.restore_defaultc
        Recursively restore default values to all members
        that have them.
        
        This method will only work for a ConfigObj that was created
        with a configspec and has been validated.
        
        It doesn't delete or modify entries without default values.
        N)
restore_defaults)
Section.restore_defaults)
TF)&r@
__iter__r
__str__r
d d!
d"d#
d$d%
d&d'
d(d)
dHd*d+
d,d-
Z d.d/
Z!d0d1
Z"d2d3
Z#d4d5
Z$d6d7
Z%d8d9
Z&d:d;
Z'd<d=
Z(dId>d?
Z)dJd@dA
Z*dBdC
Z+dDdE
)Kr!
z2An object to read, create, and write config files.a
^ # line start
        (\s*)                   # indentation
        (                       # keyword
            (?:".*?")|          # double quotes
            (?:'.*?')|          # single quotes
            (?:[^'"=].*?)       # no quotes
        )
        \s*=\s*                 # divider
        (.*)                    # value (including list values and comments)
        $   # line end
        a=
        (\s*)                     # 1: indentation
        ((?:\[\s*)+)              # 2: section marker open
        (                         # 3: section name open
            (?:"\s*\S.*?\s*")|    # at least one non-space with double quotes
            (?:'\s*\S.*?\s*')|    # at least one non-space with single quotes
            (?:[^'"\s].*?)        # at least one non-space unquoted
        )                         # section name close
        ((?:\s*\])+)              # 4: section marker close
        \s*(\#.*)?                # 5: optional comment
        $a
        (?:
            (?:
                (
                    (?:
                        (?:
                            (?:".*?")|              # double quotes
                            (?:'.*?')|              # single quotes
                            (?:[^'",\#][^,\#]*?)    # unquoted
                        )
                        \s*,\s*                     # comma
                    )*      # match all list items ending in a comma (if any)
                )
                (
                    (?:".*?")|                      # double quotes
                    (?:'.*?')|                      # single quotes
                    (?:[^'",\#\s][^,]*?)|           # unquoted
                    (?:(?<!,))                      # Empty value
                )?          # last item in a list - or string value
            )|
            (,)             # alternatively a single comma - empty list
        )
        \s*(\#.*)?          # optional comment
        $z
        (
            (?:".*?")|          # double quotes
            (?:'.*?')|          # single quotes
            (?:[^'",\#]?.*?)       # unquoted
        )
        \s*,\s*                 # comma
        a
        (
            (?:".*?")|          # double quotes
            (?:'.*?')|          # single quotes
            (?:[^'"\#].*?)|     # unquoted
            (?:)                # Empty value
        )
        \s*(\#.*)?              # optional comment
        $z
^'''(.*?)'''\s*(#.*)?$z
^"""(.*?)"""\s*(#.*)?$z
^(.*?)'''\s*(#.*)?$z
^(.*?)"""\s*(#.*)?$)
"""TF)
yesZ
trueZ
falseNc
        Parse a config file or create a config file object.
        
        ``ConfigObj(infile=None, configspec=None, encoding=None,
                    interpolation=True, raise_errors=False, list_values=True,
                    create_empty=False, file_error=False, stringify=True,
                    indent_type=None, default_encoding=None, unrepr=False,
                    write_empty_values=False, _inspec=False)``
        r
NzUPassing in an options dictionary to ConfigObj() is deprecated. Use **options instead.
stacklevelz
Unrecognised option "%s".Fr0
_inspecr
warnings
warn
DeprecationWarning
OPTION_DEFAULTSr]
_original_configspec
_load)
infile
optionsr3
_optionsr
keyword_valuer
ConfigObj.__init__c
j	rlt
}	t |	
Config file not found: "%s".
in_section
this_sectionr
set_sectionr
z$ConfigObj._load.<locals>.set_section
readz>infile must be a filename, file like object, or list of lines.
	<genexpr>
z"ConfigObj._load.<locals>.<genexpr>c
rstripr
z#ConfigObj._load.<locals>.<listcomp>z
at line %s.r
z2Parsing failed with several errors.
First error %s)#r^
filename
path
isfile
open
	readlinesr2
writerK
_errors
_handle_configspecr3
_handle_bom
endswith
newlines
allr
_parsero
errorsZ
config)
contentr
inforz
errorr
ConfigObj._loadc
BOMr%
initial_comment
final_commentr3
ConfigObj._initialisec
z#ConfigObj.__repr__.<locals>._getvalz
ConfigObj({%s})r
z&ConfigObj.__repr__.<locals>.<listcomp>r
ConfigObj.__repr__c
        Handle any BOM, and decode if necessary.
        
        If an encoding is specified, that *must* be used - but the BOM should
        still be removed (and the BOM attribute set).
        
        (If the encoding is wrongly specified, then a BOM for an alternative
        encoding won't be discovered or removed.)
        
        If an encoding is not specified, UTF8 or UTF16 BOM will be detected and
        removed. The BOM attribute will be set. UTF16 will be decoded to
        unicode.
        
        NOTE: This method must not be called with an empty ``infile``.
        
        Specifying the *wrong* encoding is likely to cause a
        ``UnicodeDecodeError``.
        
        ``infile`` must always be returned as a list of lines, but may be
        passed in as a single string.
        Nr
_decoder^
Z	text_type
BOMSr
startswith
BOM_SETr
binary_type
splitlines
decodeZ
str)
encr,
final_encoding
newliner
ConfigObj._handle_bomc
z@Decode ASCII strings to unicode if a self.encoding is specified.N)
aStringr
_a_to_u
ConfigObj._a_to_uc
        Decode infile to unicode. Using the specified encoding.
        
        if is a string, it also needs converting to a list.
        T)
	enumerate)
ConfigObj._decodec
z'Decode element to unicode if necessary.N)
_decode_element
ConfigObj._decode_elementc
        Used by ``stringify`` within validate, to turn non-string values
        into strings.
        N)
_str
ConfigObj._strc
}	|	
q2q2|
Actually parse the config file.Fr9
]z Cannot compute the section depthz
Cannot compute nesting levelz
Section too nestedz
Duplicate section name)
z=Invalid line ({0!r}) (matched as neither section nor keyword)
Parse error in multiline valuerm
Unknown name or type in valuez+Parse error from unrepr-ing multiline valuez!Parse error from unrepr-ing valuez
Parse error in valuez
Duplicate keyword name)
)#r0
stripr1
_sectionmarkerr
groupsr5
count
_handle_errorr
_match_depthrf
_unquoter
_keyword
formatr
_multiliner
typer)
_handle_valuer
temp_list_valuesZ
comment_listZ
done_startr
maxline
	cur_indexZ
reset_commentrn
sline
indentZ	sect_openZ	sect_nameZ
sect_close
commentZ	cur_depthrf
ConfigObj._parsec
        Given a section and a depth level, walk back through the sections
        parents to see if the depth level matches a previous section.
        
        Return a reference to the right section,
        or raise a SyntaxError.
        N)
sectr
ConfigObj._match_depthc
        Handle an error according to the error settings.
        
        Either raise the error or store it.
        The error will have occured at ``cur_index``
        r9
{0} at line {1}.N)
textZ
ErrorClassr
ConfigObj._handle_errorc
z%Return an unquoted version of a valuer
ConfigObj._unquotec
        Return a safely quoted version of a value.
        
        Raise a ConfigObjError if the value cannot be safely quoted.
        If multiline is ``True`` (default) then use triple quotes
        if necessary.
        
        * Don't quote values that don't need it.
        * Recursively quote members of a list and return a comma joined list.
        * Multiline is ``False`` for lists.
        * Obey list syntax for empty and single member lists.
        
        If ``list_values=False`` then the value is only quoted if it contains
        a ``\n`` (is multiline) or '#'.
        
        If ``write_empty_values`` is set, and the value is an empty string, it
        won't be quoted.
        rm
	multiliner
_quote
z$ConfigObj._quote.<locals>.<listcomp>z
Value "%s" is not a string.
#Value "%s" cannot be safely quoted.r
noquotr
wspace_plus
_get_single_quote
_get_triple_quote)
no_lists_no_quotesZ
need_tripleZ
hash_triple_quoteZ
check_for_single
quotr
ConfigObj._quotec
squot
dquot
ConfigObj._get_single_quotec
findr
tdquot
tsquotre
ConfigObj._get_triple_quotec
        Given a value string, unquote, remove comment,
        handle lists. (including empty and single member lists)
        rm
z+ConfigObj._handle_value.<locals>.<listcomp>)
_nolistvaluer
	_valueexprH
_listvalueexp
findall)
singleZ
empty_listrR
ConfigObj._handle_valuec
}	|	d
}	|	d
z9Extract the value, where we are in a multiline situation.NrA
_triple_quoter
newvalueZ
single_lineZ
multi_linerP
retvalrn
ConfigObj._multilinec
Parse the configspec.T)
Parsing configspec failed: %sNz
Reading configspec failed: %s)
ConfigObj._handle_configspecc
rLq>|
        Called by validate. Handles setting the configspec on subsections
        including sections to be validated by __many__
        
__many__Trm
N)	r3
copyr3
manyr
_set_configspec
ConfigObj._set_configspecc
z.Write an individual line, for the write method
%s%s%s%s%sFrX
 = )
indent_stringr
_write_line
ConfigObj._write_linec
Write a section marker linerr
_write_marker
ConfigObj._write_markerc
Deal with a comment.rm
 # )
_handle_comment
ConfigObj._handle_commentc
p<t j!}
rvt$j%d
t(j)
n8t-
        Write the current ConfigObj as a file
        
        tekNico: FIXME: use StringIO instead of real files
        
        >>> filename = a.filename
        >>> a.filename = 'test.ini'
        >>> a.write()
        >>> a.filename = filename
        >>> a == ConfigObj('test.ini', raise_errors=True)
        1
        >>> import os
        >>> os.remove('test.ini')
        Nr>
# Fr~
encoder
z#ConfigObj.write.<locals>.<listcomp>r
moder
win32r
ascii
wb).r5
lstriprv
extendr 
lineseprd
platformr
outfiler
cspZ
int_valrn
stripped_liners
comment_liner
outputZ
output_bytesr)
ConfigObj.writec
q^|	
        Test the ConfigObj against a configspec.
        
        It uses the ``validator`` object from *validate.py*.
        
        To run ``validate`` on the current ConfigObj, call: ::
        
            test = config.validate(validator)
        
        (Normally having previously passed in the configspec when the ConfigObj
        was created - you can dynamically assign a dictionary of checks to the
        ``configspec`` attribute of a section though).
        
        It returns ``True`` if everything passes, or a dictionary of
        pass/fails (True/False). If every member of a subsection passes, it
        will just have the value ``True``. (It also returns ``False`` if all
        members fail).
        
        In addition, it converts the values from strings to their native
        types if their checks pass (and ``stringify`` is set).
        
        If ``preserve_errors`` is ``True`` (``False`` is default) then instead
        of a marking a fail with a ``False``, it will preserve the actual
        exception object. This can contain info about the reason for failure.
        For example the ``VdtValueTooSmallError`` indicates that the value
        supplied was too small. If a value (or section) is missing it will
        still be marked as ``False``.
        
        You must have the validate module to use ``preserve_errors=True``.
        
        You can then use the ``flatten_errors`` function to turn your nested
        results dictionary into a flattened list of failures - useful for
        displaying meaningful error messages.
        Nz
No configspec supplied.r
VdtMissingValuec
missingFTc
itemrw
z>ConfigObj.validate.<locals>.validate_entry.<locals>.<listcomp>rm
get_default_valuer
baseErrorClass
checkr^
_vdtMissingValuer4
specr
ret_true
	ret_falser
preserve_errorsr
	validatorr
validate_entry
z*ConfigObj.validate.<locals>.validate_entryTc
z&ConfigObj.validate.<locals>.<listcomp>c
___many___rm
z"Value %r was provided as a sectionz)Section %r was provided as a single valuer
validater
anyr
unvalidatedZ
incorrect_sectionsZ
incorrect_scalarsr
ConfigObj.validatec
z@Clear ConfigObj instance and restore to 'freshly created' state.N)
reset"	
ConfigObj.resetc
r0q"t
        Reload a ConfigObj from file.
        
        This method raises a ``ReloadError`` if the ConfigObj doesn't have
        a filename attribute pointing to a file.
        r3
current_optionsr
reload-	
ConfigObj.reload)
NNNNTFTFFTNNFFF)
FFN)-r@
_single_line_singleZ
_single_line_doubleZ
_multi_line_singleZ
_multi_line_doublerm
)	r"
    A simple validator.
    Can be used to check that all members expected are present.
    
    To use it, provide a configspec with all your members in (the value given
    will be ignored). Pass an instance of ``SimpleVal`` to the ``validate``
    method of your ``ConfigObj``. ``validate`` will return ``True`` if all
    members are present, or a dictionary with True/False meaning
    present/missing. (Whole missing sections will be replaced with ``False``)
    c
SimpleVal.__init__Fc
z9A dummy check method, always returns the value unchanged.)
memberr
SimpleVal.checkN)
r|qjt
    An example function that will turn a nested dictionary of results
    (as returned by ``ConfigObj.validate``) into a flat list.
    
    ``cfg`` is the ConfigObj instance being checked, ``res`` is the results
    dictionary returned by ``validate``.
    
    (This is a recursive function, so you shouldn't use the ``levels`` or
    ``results`` arguments - they are used by the function.)
    
    Returns a list of keys that failed. Each member of the list is a tuple::
    
        ([list of sections...], key, result)
    
    If ``validate`` was called with ``preserve_errors=False`` (the default)
    then ``result`` will always be ``False``.
    *list of sections* is a flattened list of sections that the key was found
    in.
    
    If the section was missing (or a section was expected and a scalar provided
    - or vice-versa) then key will be ``None``.
    
    If the value (or section) was missing then ``result`` will be ``False``.
    
    If ``validate`` was called with ``preserve_errors=True`` and a value
    was present, but failed the check, then ``result`` will be the exception
    object returned. You can use this as a string that describes the failure.
    
    For example *The value "3" is of the wrong type*.
    NTF)
sortedr^
resZ
levels
resultsr
    Find all the values and sections not in the configspec from a validated
    ConfigObj.
    
    ``get_extra_values`` returns a list of tuples where each tuple represents
    either an extra section, or an extra value.
    
    The tuples contain two values, a tuple representing the section the value 
    is in and the name of the extra values. For extra values in the top level
    section the first member will be an empty tuple. For values in the 'foo'
    section the first member will be ``('foo',)``. For members in the 'bar'
    subsection of the 'foo' section the first member will be ``('foo', 'bar')``.
    
    NOTE: If you call ``get_extra_values`` on a ConfigObj instance that hasn't
    been validated it will return an empty list.
    c
_prependr
z$get_extra_values.<locals>.<listcomp>)
confr
codecsr
_versionr
objectr
__all__r
MAX_INTERPOL_DEPTHr
_builderr7
<module>
