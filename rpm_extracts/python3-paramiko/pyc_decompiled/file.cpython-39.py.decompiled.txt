m	Z	m
linefeed_byte_value
crlf
cr_byte
linefeed_byte
cr_byte_value)
BytesIO
bytes_types
	text_type)
ClosingContextManagerc
dBd"d#
dCd$d%
dDd&d'
dEd(d)
d*d+
d,d-
Z d.d/
Z!d0d1
Z"e#d2d3
Z$d4d5
Z%d6d7
Z&d8d9
Z'dFd<d=
Z(d>d?
Z)d@dA
Z*d!S
BufferedFilezc
    Reusable base class to implement Python-style file buffering around a
    simpler stream.
    i
newlines
_flags
_DEFAULT_BUFSIZE
_bufsizer
_wbuffer
bytes
_rbuffer
_at_trailing_cr
_closed
_pos
_realpos
_size
self
1/usr/lib/python3.9/site-packages/paramiko/file.py
__init__2
BufferedFile.__init__c
closer!
__del__A
BufferedFile.__del__c
        Returns an iterator that can be used to iterate over the lines in this
        file.  This iterator happens to return the file itself, since a file is
        its own iterator.
        :raises: ``ValueError`` -- if the file is closed.
        z
I/O operation on closed file)
ValueErrorr!
__iter__D
BufferedFile.__iter__c
        Close the file.  Future read and write operations will fail.
        TN)
flushr
BufferedFile.closec
        Write out any data in the write buffer.  This may do nothing if write
        buffering is not turned on.
        N)
_write_allr
getvaluer
BufferedFile.flushc
            Returns the next line from the input, or raises
            ``StopIteration`` when EOF is hit.  Unlike Python file
            objects, it's okay to mix calls to `next` and `readline`.
            :raises: ``StopIteration`` -- when the end of the file is reached.
            :returns: a line (`str`) read from the file.
            
readline
StopIteration
liner#
nextb
BufferedFile.nextc
            Returns the next line from the input, or raises ``StopIteration``
            when EOF is hit.  Unlike python file objects, it's okay to mix
            calls to `.next` and `.readline`.
            :raises: ``StopIteration`` -- when the end of the file is reached.
            :returns: a line (`str`) read from the file.
            r.
__next__s
BufferedFile.__next__c
        Check if the file can be read from.
        :returns:
            `True` if the file can be read from. If `False`, `read` will raise
            an exception.
        )
	FLAG_READr!
readable
BufferedFile.readablec
        Check if the file can be written to.
        :returns:
            `True` if the file can be written to. If `False`, `write` will
            raise an exception.
        )
FLAG_WRITEr!
writable
BufferedFile.writablec
        Check if the file supports random access.
        :returns:
            `True` if the file supports random access. If `False`, `seek` will
            raise an exception.
        Fr#
seekable
BufferedFile.seekablec
        Read up to ``len(buff)`` bytes into ``bytearray`` *buff* and return the
        number of bytes read.
        :returns:
            The number of bytes read.
        N)
read
len)
buff
datar#
readinto
BufferedFile.readintoNc
        Read at most ``size`` bytes from the file (less if we hit the end of
        the file first).  If the ``size`` argument is negative or omitted,
        read all the remaining data in the file.
        .. note::
            ``'b'`` mode flag is ignored (``self.FLAG_BINARY`` in
            ``self._flags``), because SSH treats all files as binary, since we
            have no idea what encoding the file is in, or even if the file is
            text data.
        :param int size: maximum number of bytes to read
        :returns:
            data read from the file (as bytes), or an empty string if EOF was
            encountered immediately
        
File is closedz
File is not open for readingNr
IOErrorr
	bytearrayr
_readr
EOFError
extendr
FLAG_BUFFERED
maxr
size
result
new_dataZ	read_sizer#
BufferedFile.readc
        Read one entire line from the file.  A trailing newline character is
        kept in the string (but may be absent when a file ends with an
        incomplete line).  If the size argument is present and non-negative, it
        is a maximum byte count (including the trailing newline) and an
        incomplete line may be returned.  An empty string is returned only when
        EOF is encountered immediately.
        .. note::
            Unlike stdio's ``fgets``, the returned string contains null
            characters (``'\0'``) if they occurred in the input.
        :param int size: maximum length of returned string.
        :returns:
            next line of the file, or an empty string if the end of the
            file has been reached.
            If the file was opened in binary (``'b'``) mode: bytes are returned
            Else: the encoding of the file is assumed to be UTF-8 and character
            strings (`str`) are returned
        r>
File not open for readingFr
FLAG_UNIVERSAL_NEWLINEr;
_record_newliner
FLAG_BINARYr	
findr
Z	truncated
posZ
rposZ
xposZ
lfr#
BufferedFile.readlinec
        Read all remaining lines using `readline` and return them as a list.
        If the optional ``sizehint`` argument is present, instead of reading up
        to EOF, whole lines totalling approximately sizehint bytes (possibly
        after rounding up to an internal buffer size) are read.
        :param int sizehint: desired maximum number of bytes to read.
        :returns: list of lines read from the file.
        r
append)
sizehint
linesZ
byte_countr2
	readlinesP
BufferedFile.readlinesc
        Set the file's current position, like stdio's ``fseek``.  Not all file
        objects support seeking.
        .. note::
            If a file is opened in append mode (``'a'`` or ``'a+'``), any seek
            operations will be undone at the next write (as the file position
            will move back to the end of the file).
        :param int offset:
            position to move to within the file, relative to ``whence``.
        :param int whence:
            type of movement: 0 = absolute; 1 = relative to the current
            position; 2 = relative to the end of the file.
        :raises: ``IOError`` -- if the file doesn't support random access.
        z
File does not support seeking.N
offset
whencer#
seekf
BufferedFile.seekc
        Return the file's current position.  This may not be accurate or
        useful if the underlying file doesn't support random access, or was
        opened in append mode.
        :returns: file position (`number <int>` of bytes).
        )
tellz
BufferedFile.tellc
        Write data to the file.  If write buffering is on (``bufsize`` was
        specified and non-zero), some or all of the data may not actually be
        written yet.  (Use `flush` or `close` to force buffered data to be
        written out.)
        :param data: ``str``/``bytes`` data to write
        z
utf-8r>
File not open for writingNr
isinstancer
encoder
write
FLAG_LINE_BUFFERED
rfindr
last_newline_posZ
wbufr#
BufferedFile.writec
        Write a sequence of strings to the file.  The sequence can be any
        iterable object producing strings, typically a list of strings.  (The
        name is intended to match `readlines`; `writelines` does not add line
        separators.)
        :param sequence: an iterable sequence of strings.
        N)
sequencer2
writelines
BufferedFile.writelinesc
        Identical to ``iter(f)``.  This is a deprecated file interface that
        predates Python iterator support.
        r#
xreadlines
BufferedFile.xreadlinesc
closed
BufferedFile.closedc
        (subclass override)
        Read data from the stream.  Return ``None`` or raise ``EOFError`` to
        indicate EOF.
        N)
BufferedFile._readc
        (subclass override)
        Write data into the stream.
        z
write not implementedNrT
_write
BufferedFile._writec
        (subclass override)
        Return the size of the file.  This is called from within `_set_mode`
        if the file is opened in append mode, so the file position can be
        tracked and `seek` and `tell` will work correctly.  If the file is
        a stream that can't be randomly accessed, you don't need to override
        this method,
        r
	_get_size
BufferedFile._get_size
        Subclasses call this method to initialize the BufferedFile.
        r
FLAG_APPENDrb
mode
bufsizer#
	_set_mode
BufferedFile._set_modec
memoryviewr;
raw_datar<
countr#
BufferedFile._write_allc
newliner#
BufferedFile._record_newline)
__name__
__module__
__qualname__
__doc__r
SEEK_SET
SEEK_CUR
SEEK_ENDr5
propertyr`
paramiko.commonr
paramiko.py3compatr
paramiko.utilr
<module>
