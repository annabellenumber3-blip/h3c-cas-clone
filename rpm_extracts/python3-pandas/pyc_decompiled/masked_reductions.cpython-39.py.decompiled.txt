m	Z	
masked_reductions.py is for reduction algorithms using a mask-based approach
for missing values.
CallableN)
missing)
check_below_min_countT)
skipna
	min_count
func
values
maskr
    Sum or product for 1D masked array.
    Parameters
    ----------
    func : np.sum or np.prod
    values : np.ndarray
        Numpy array with the values (can be of any dtype that support the
        operation).
    mask : np.ndarray
        Boolean numpy array (True values indicate missing values).
    skipna : bool, default True
        Whether to skip NA.
    min_count : int, default 0
        The required number of valid values to perform the operation. If fewer than
        ``min_count`` non-NA values are present the result will be NA.
    N)
where)
anyr
shape
libmissing
O/usr/lib64/python3.9/site-packages/pandas/core/array_algos/masked_reductions.py
_sumprod
sumr
prodr
    Reduction for 1D masked array.
    Parameters
    ----------
    func : np.min or np.max
    values : np.ndarray
        Numpy array with the values (can be of any dtype that support the
        operation).
    mask : np.ndarray
        Boolean numpy array (True values indicate missing values).
    skipna : bool, default True
        Whether to skip NA.
    N)
sizer
subsetr
_minmaxC
minr
maxr
allr
count_nonzero)
_sum
countZ
mean_valuer
meank
__doc__
typingr
numpyr
pandas._libsr
pandas.core.nanopsr
ndarray
bool
intr
<module>
